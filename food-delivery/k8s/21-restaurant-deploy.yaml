apiVersion: apps/v1
kind: Deployment
metadata:
  name: testbed-restaurant
  namespace: rca-testbed-food
  labels:
    app: testbed-restaurant
spec:
  replicas: 1
  selector:
    matchLabels:
      app: testbed-restaurant
  template:
    metadata:
      labels:
        app: testbed-restaurant
    spec:
      containers:
        - name: restaurant-service
          image: food-delivery-restaurant:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 8081
          envFrom:
            - configMapRef:
                name: service-config
          env:
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: mysql-secret
                  key: MYSQL_USER
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: mysql-secret
                  key: MYSQL_PASSWORD
          startupProbe:
            httpGet:
              path: /actuator/health
              port: 8081
            failureThreshold: 30
            periodSeconds: 5
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8081
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8081
            periodSeconds: 15
            timeoutSeconds: 3
            failureThreshold: 5
          resources:
            requests:
              cpu: 200m
              memory: 512Mi
            limits:
              cpu: 500m
              memory: 1Gi
---
apiVersion: v1
kind: Service
metadata:
  name: testbed-restaurant
  namespace: rca-testbed-food
spec:
  selector:
    app: testbed-restaurant
  ports:
    - port: 8081
      targetPort: 8081
