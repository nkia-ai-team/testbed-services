apiVersion: apps/v1
kind: Deployment
metadata:
  name: testbed-dispatch
  namespace: rca-testbed-food
  labels:
    app: testbed-dispatch
spec:
  replicas: 1
  selector:
    matchLabels:
      app: testbed-dispatch
  template:
    metadata:
      labels:
        app: testbed-dispatch
    spec:
      containers:
        - name: dispatch-service
          image: food-delivery-dispatch:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 8082
          envFrom:
            - configMapRef:
                name: service-config
          env:
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: mysql-secret
                  key: MYSQL_USER
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: mysql-secret
                  key: MYSQL_PASSWORD
          startupProbe:
            httpGet:
              path: /actuator/health
              port: 8082
            failureThreshold: 30
            periodSeconds: 5
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8082
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8082
            periodSeconds: 15
            timeoutSeconds: 3
            failureThreshold: 5
          resources:
            requests:
              cpu: 200m
              memory: 512Mi
            limits:
              cpu: 500m
              memory: 1Gi
---
apiVersion: v1
kind: Service
metadata:
  name: testbed-dispatch
  namespace: rca-testbed-food
spec:
  selector:
    app: testbed-dispatch
  ports:
    - port: 8082
      targetPort: 8082
