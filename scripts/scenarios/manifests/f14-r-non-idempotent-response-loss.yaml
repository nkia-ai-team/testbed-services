{
  "schema_version": "1.0",
  "id": "F14-R",
  "slug": "f14-r-non-idempotent-response-loss",
  "readiness": "parked",
  "authoritative_sources": {
    "catalog": "scripts/scenarios/catalog.json",
    "execution_matrix": "docs/spec-scenario-execution-matrix.md"
  },
  "injection": {
    "location": "scenario-runner",
    "matrix_location_transport": "K ingress/fault image",
    "transport": "local-orchestrator",
    "profile_refs": [
      "injector-profiles/timeline.compose",
      "injector-profiles/load.north_south"
    ]
  },
  "execution": {
    "preflight_ids": [
      "catalog-integrity",
      "canonical-kubeconfig",
      "global-dirty-lease",
      "baseline-clean-window",
      "target-health",
      "profile-prerequisites",
      "transport-local-runner-state",
      "subinjection-timeline",
      "baseline-loadgen-active"
    ],
    "controller": {
      "dispatcher_mode": "dry-run",
      "decision_mode": "evaluation",
      "profile": {
        "kind": "fixed",
        "ref": "controller-profiles/f14-r-non-idempotent-response-loss/fixed",
        "requires_approved_fixed_profile": true
      },
      "live_enabled": false
    }
  },
  "actions": {
    "plan": {
      "mode": "dry-run",
      "mutation": false,
      "adapter_refs": [
        "timeline.compose",
        "load.north_south"
      ]
    },
    "run": {
      "mode": "dry-run",
      "mutation": false,
      "requires_preflight": true,
      "adapter_refs": [
        "timeline.compose",
        "load.north_south"
      ]
    },
    "cleanup": {
      "mode": "dry-run",
      "mutation": false,
      "required": true,
      "order": "reverse",
      "recovery_gate": true,
      "adapter_refs": [
        "load.north_south",
        "timeline.compose"
      ]
    }
  },
  "prerequisite_gate": {
    "state": "unresolved",
    "required": true,
    "description": "2026-07-29 실측 반증. 중복이 생기려면 1차 시도가 커밋되면서(각 하류 타임아웃 PG 10s·뱅킹 10s 이내) 동시에 order 측 15s를 넘겨야 하는데, pg-mock 한 곳만 늦추면 총 소요가 10s를 못 넘어 재시도 자체가 발생하지 않는다. 15s 이상으로 올리면 PG 구간이 먼저 터져 @Transactional이 롤백되어 결제 행이 남지 않는다. 성립하려면 PG 9s + 뱅킹 이체 8s 동시 주입이 필요하나 뱅킹 이체 구간에 유한·제어 가능한 지연 수단이 없다 (db.lock은 해제까지 대기라 10s 초과). 앵커 자체는 실재 — PaymentService.processPayment 멱등성 부재 × PaymentClient.java:28 @Retry(max-attempts=2)",
    "live_allowed": false
  },
  "capture_policy": {
    "policy_ref": "focused-window-v1",
    "time_basis": "UTC",
    "t1": "earliest-actual-effect-start",
    "t2": "latest-load-end-or-fault-release",
    "query_window": "[t1-10m,t2+20m]",
    "export_not_before": "t2+20m",
    "model_snapshot": "/var/lib/lucida/ai-models/stream-anomaly/global/v1/model.json@t2+20m",
    "create_golden_anomaly": false,
    "allowed_case_labels": [
      "calibration",
      "evaluation",
      "failed"
    ]
  }
}
