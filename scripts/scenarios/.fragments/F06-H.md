# Fragment: F06-H — payment 내부 DB 테이블 잠금 (live 승격)

**준비도 판정: READY.** evaluation/fixed. primary=db.lock, companion=load.north_south.
정적 검증: 패치 인메모리 적용 후 `validate_contracts` + `build_manifest` + `validate_manifest` 통과(live_enabled=True, gate=True). 라이브 주입/실측 없음.

**직접 반영 완료(개별 파일):**
- `profiles/db_lock_executor.py` — CONTRACTS["F06-H"]를 row-lock→테이블 EXCLUSIVE 잠금으로 교체(payments는 신규 INSERT라 row lock 불가, 07-20 실측). POSTGRES_POD_REMOTE가 `LOCK TABLE payment_schema.payments IN EXCLUSIVE MODE`를 수행(평문 SELECT/actuator health는 통과 → pod readiness 자기-abort 회피). lock_mode allowlist로 SQL 주입 표면 차단. application_name 접미사 하드코딩(`-inventory-lock`)을 `rca-<sid>-*-lock` 파라미터화로 일반화(F01-R 계약 무손상, 지뢰#감사).
- `tests/test_f06h_db_table_lock.py` — 신규(6 케이스, 로컬 통과).
- `tests/test_db_host_executor_foundations.py` — 기존 F06-H FOR UPDATE 단언을 테이블 잠금 단언으로 갱신(집행기 변경에 직접 결속된 단언, 로컬 98 통과).

아래는 **공유 파일 기계 적용 패치**(직접 수정 금지 대상).

## 1. catalog.json — F06-H 항목
- `readiness`: `"partial"` → `"ready"`
- `prerequisite`: `"payment row before outbound call"` → `"payments table exclusive lock blocks payment writes"`
- profiles 불변: `["db.lock","load.north_south"]` (load_mode `"fixed"` 유지)

## 2. registry/profiles.json
### db.lock
- `parameter_contract.allowed_scenarios`: `["F01-R"]` → `["F01-R","F06-H"]`
- `scenario_parameters["F06-H"]` 추가(= db_lock_executor CONTRACTS["F06-H"]와 정확히 동일해야 함):
```json
{
  "engine": "postgresql",
  "namespace": "rca-testbed-commerce",
  "pod": "testbed-postgres-0",
  "schema": "payment_schema",
  "table": "payments",
  "lock_scope": "table",
  "lock_mode": "EXCLUSIVE",
  "application_name": "rca-F06-H-payment-lock",
  "hold_seconds": 600
}
```
### load.north_south
- `parameter_contract.allowed_scenarios` 끝에 `"F06-H"` 추가
- `parameter_contract.tag_pattern`을 아래로 교체(기존 F07-P/F08-P/F09-R 누락분까지 동시 폐색 — 오늘 감사 누락 사고 실증, 지뢰#6):
```
^scenario_id=F(07-H|03-G|01-R|01-H|01-G|05-G|06-R|08-H|09-P|11-R|11-G|02-R|04-R|12-H|06-G|05-R|05-H|07-P|08-P|09-R|03-P|06-H)$
```
- `scenario_parameters["F06-H"]` 추가:
```json
{
  "target_rps": 35,
  "ramp_up": "2m",
  "hold": "8m",
  "ramp_down": "1m",
  "entry_url": "http://192.168.122.77:30080",
  "script_path": "/opt/loadgen/commerce/surge.js",
  "scenario_tag": "scenario_id=F06-H",
  "seed": 4242,
  "baseline_unit": "loadgen-commerce"
}
```

## 3. registry/controllers.json
- `live_scenario_ids` 끝에 `"F06-H"` 추가(F03-P와 함께: `... ,"F09-R","F06-H","F03-P"`)
- `controllers["F06-H"]` 추가:
```json
{
  "dispatcher_mode": "trusted",
  "live_enabled": true,
  "mode": "evaluation",
  "tick_interval": "15s",
  "settle_after_change": "30s",
  "max_injection_duration": "12m",
  "preflight": [
    "kube-context",
    "kube-node-set",
    "coordinator-clean",
    "clean-window",
    "baseline-traffic",
    "target-health"
  ],
  "baseline": {
    "clean_window": "2h",
    "required": [
      {
        "check": "coordinator-clean"
      },
      {
        "check": "clean-window"
      },
      {
        "check": "baseline-traffic"
      },
      {
        "check": "target-health"
      }
    ]
  },
  "profile": {
    "kind": "fixed",
    "primary_ref": "db.lock",
    "companion_refs": [
      "load.north_south"
    ],
    "approved_profile_id": "db.lock",
    "levels": [
      {
        "id": "approved-fixed-f06-h",
        "parameters": {
          "engine": "postgresql",
          "namespace": "rca-testbed-commerce",
          "pod": "testbed-postgres-0",
          "schema": "payment_schema",
          "table": "payments",
          "lock_scope": "table",
          "lock_mode": "EXCLUSIVE",
          "application_name": "rca-F06-H-payment-lock",
          "hold_seconds": 600
        },
        "min_hold": "4m",
        "settle": "30s",
        "timeout": "10m"
      }
    ]
  },
  "observations": [
    {
      "id": "entry_status",
      "adapter": "http_probe",
      "query_id": "http.entry_health",
      "freshness": "15s"
    },
    {
      "id": "target_health",
      "adapter": "http_probe",
      "query_id": "http.target_health",
      "freshness": "15s"
    },
    {
      "id": "pod_ready",
      "adapter": "kubernetes",
      "query_id": "kubernetes.pod_ready",
      "parameters": {
        "namespace": "rca-testbed-commerce",
        "resource": "testbed-payment"
      },
      "freshness": "30s"
    },
    {
      "id": "tagged_session_count",
      "adapter": "database",
      "query_id": "database.tagged_session_count",
      "freshness": "30s"
    },
    {
      "id": "order_error_rate",
      "adapter": "prometheus",
      "query_id": "prometheus.apm_service_error_rate",
      "parameters": {
        "service_name": "commerce-order"
      },
      "freshness": "60s"
    },
    {
      "id": "payment_p95",
      "adapter": "prometheus",
      "query_id": "prometheus.apm_service_p95",
      "parameters": {
        "service_name": "commerce-payment"
      },
      "freshness": "120s"
    },
    {
      "id": "achieved_rps",
      "adapter": "loadgen_summary",
      "query_id": "loadgen.achieved_rps",
      "freshness": "30s"
    },
    {
      "id": "checkout_5xx_rate",
      "adapter": "loadgen_summary",
      "query_id": "loadgen.checkout_5xx_rate",
      "freshness": "30s"
    }
  ],
  "success": {
    "all": [
      {
        "id": "db-lock-held",
        "observation": "tagged_session_count",
        "op": "gte",
        "value": 1
      },
      {
        "id": "checkout-impact",
        "observation": "checkout_5xx_rate",
        "op": "gte",
        "value": 0.05
      },
      {
        "id": "order-timeout-symptom",
        "observation": "order_error_rate",
        "op": "gte",
        "value": 0.1
      },
      {
        "id": "controlled-user-load",
        "observation": "achieved_rps",
        "op": "gte",
        "value": 15
      }
    ],
    "consecutive_ticks": 3
  },
  "must_rule_out": {
    "any": [
      {
        "id": "symptom-without-our-lock",
        "observation": "tagged_session_count",
        "op": "eq",
        "value": 0
      }
    ],
    "consecutive_ticks": 3
  },
  "abort": {
    "any": [
      {
        "id": "entry-unreachable",
        "observation": "entry_status",
        "op": "eq",
        "value": 0
      },
      {
        "id": "pod-unready",
        "observation": "pod_ready",
        "op": "eq",
        "value": false
      }
    ],
    "consecutive_ticks": 2
  },
  "recovery": {
    "all": [
      {
        "id": "target-recovered",
        "observation": "target_health",
        "op": "eq",
        "value": 200
      },
      {
        "id": "pod-recovered",
        "observation": "pod_ready",
        "op": "eq",
        "value": true
      },
      {
        "id": "lock-released",
        "observation": "tagged_session_count",
        "op": "eq",
        "value": 0
      }
    ],
    "consecutive_ticks": 2,
    "timeout": "10m"
  },
  "cleanup": {
    "required": true,
    "order": "reverse",
    "timeout": "10m"
  },
  "capture": {
    "enabled": true,
    "pre_window": "2h",
    "post_window": "45m",
    "model_snapshot": "/var/lib/lucida/ai-models/stream-anomaly/global/v1/model.json",
    "create_golden_anomaly": false
  }
}
```

## 4. registry/scenario-metadata.json — scenarios에 병합
```json
{
  "F06-H": {
    "title": "payment 내부 DB 테이블 잠금(외부 timeout 감별짝)",
    "description": "commerce payment의 결제 쓰기 경로를 payment_schema.payments 테이블 EXCLUSIVE 잠금으로 막아, 외부 결제 hang(F06-R)과 동일한 증상(checkout 결제 확정 실패)을 내부 DB 원인으로 재현하는 시나리오다. 원인은 외부 PG·네트워크가 아니라 DB 내부 테이블 잠금이다.",
    "cause": "Internal PostgreSQL table lock on payments (tagged session, EXCLUSIVE mode)",
    "injection_summary": "tb-runner→PG NodePort 경유가 아니라 testbed-postgres-0 pod 안에서 PGAPPNAME=rca-F06-H-payment-lock 세션이 BEGIN; LOCK TABLE payment_schema.payments IN EXCLUSIVE MODE; pg_sleep(600); ROLLBACK로 payments 테이블을 잠근다. payment INSERT(ROW EXCLUSIVE)는 블록되지만 평문 SELECT/actuator health(SELECT 1, ACCESS SHARE)는 통과해 pod readiness는 유지된다. north-south 35rps companion이 checkout 결제 경로를 흘려 증상을 발현시킨다. cleanup은 tagged 세션 pg_terminate_backend+원복(멱등).",
    "user_impact": "checkout의 결제 확정 단계에서 payment INSERT가 잠금 대기하다 order의 payment read-timeout(15s)을 넘겨 timeout으로 실패, 주문 실패율(5xx)이 상승한다.",
    "distinguishing_evidence": "pg_stat_activity에 application_name=rca-F06-H-payment-lock 세션이 상주하고(원인 신호), payment_p95는 급등하지만 payment error_rate는 (요청이 에러가 아니라 잠금 대기라) 반드시 오르지는 않는다. F06-R(외부 payment hang)과 달리 외부 PG·mock·네트워크는 정상이고 DB 내부 테이블 잠금이 원인이다. payments는 결제마다 신규 INSERT라 특정 row FOR UPDATE로는 재현 불가 — 테이블 잠금이 유일 표면(07-20 실측)."
  }
}
```

## 5. 공유 테스트 갱신(fragment 적용 전제 — 목록)
- `tests/test_registry_contracts.py`
  - `test_all_64_scenarios_compile_with_twenty_trusted_live_plans`: live_ids set에 `"F06-H"` 추가
  - `test_exactly_twenty_manifests_have_complete_live_controllers`: 기대 set에 `"F06-H"` 추가, live_scenario_ids 리스트 끝에 `"F06-H","F03-P"` 추가
  - `test_f05_payment_faults_are_exact_and_causally_distinct`: `live_scenario_ids[-2:] == ["F08-P","F09-R"]` → `== ["F06-H","F03-P"]`
- `tests/test_ready_profile_executors.py`
  - `test_live_matrix_has_twenty_ready_plans_with_payment_faults`: 기대 set에 `"F06-H"` 추가
  (주의: 이 갱신들은 F03-P 프래그먼트와 공유 — 두 프래그먼트 적용 시 한 번만 반영)

## 6. runner-side (109 `/root/rca-scenario-runner/backend/app/live_probes.py`) — 라이브 전 적용, 재빌드 필요
- `APPROVED_APM_SERVICES`에 `"commerce-payment"` 추가 — payment_p95 관측용(현재 미포함, order APM은 이미 허용됨). **미적용 시 payment_p95가 quality=error → freshness 실패.**
- `testbed-payment` k8s target·`database.tagged_session_count` 프로브는 이미 허용(F01-R 사용). F06-H 태그(rca-F06-H-payment-lock)로 tagged_session_count가 집계되는지 라이브 전 1회 확인.

## 7. 재생성 + 검증
```
python3 scripts/scenarios/generate-manifests.py        # manifests/f06-h-*.yaml 재생성(직접 편집 금지)
python3 -m pytest scripts/scenarios/tests -q
```

## 8. 지뢰 점검 매핑
- #1 timeout(10m)>ramp2m+hold8m+down1m=11m? → hold 8m, max_injection 12m로 가드. level.timeout 10m > min_hold 4m. #2 min_hold 4m>settle 30s. #3 evaluation(escalate 없음). #4 **abort pod_ready eq false는 compile-plan이 강제(entry_status==0 ∧ pod-gate)** — 테이블 EXCLUSIVE 잠금은 readiness(SELECT 1)를 막지 않아 자기-트리거 안 함(consecutive 2). #6 tag_pattern/allowlist 등록 명시(§2). #9 cleanup=pg_terminate_backend+rm+멱등. #11 expected_anomalies precondition: commerce-payment/commerce-order APM coverage(ai_coverages) 등록 확인 + payment INSERT 지연 로그가 ERROR인지 라이브 전 확인.
