apiVersion: apps/v1
kind: Deployment
metadata:
  name: testbed-account
  namespace: rca-testbed-banking
  labels:
    app: testbed-account
spec:
  replicas: 1
  selector:
    matchLabels:
      app: testbed-account
  template:
    metadata:
      labels:
        app: testbed-account
    spec:
      containers:
        - name: account-service
          image: core-banking-account:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 8081
          envFrom:
            - configMapRef:
                name: service-config
          env:
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: oracle-secret
                  key: APP_USER
            - name: DB_PASS
              valueFrom:
                secretKeyRef:
                  name: oracle-secret
                  key: APP_USER_PASSWORD
          startupProbe:
            httpGet:
              path: /actuator/health
              port: 8081
            failureThreshold: 30
            periodSeconds: 5
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8081
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8081
            periodSeconds: 15
            timeoutSeconds: 3
            failureThreshold: 5
          resources:
            requests:
              cpu: 200m
              memory: 512Mi
            limits:
              cpu: 500m
              memory: 1Gi
---
apiVersion: v1
kind: Service
metadata:
  name: testbed-account
  namespace: rca-testbed-banking
spec:
  selector:
    app: testbed-account
  ports:
    - port: 8081
      targetPort: 8081
