# 배치 후 수리 백로그 (2026-08-07 야간 배치 `live-41-58c5b3e5`)

목표: **다음 주 출근 시 41종이 전부 문제없이 돈다.**
배치가 도는 중에 확정된 것만 적는다. 배치 완주 전에는 클러스터를 바꾸지 않는다.

---

# 현재 상태 요약 (2026-08-08 작성 · 배치 37/41 시점)

> 이 문서는 하루 동안 20여 커밋으로 커졌고 **정정이 여러 겹 쌓여 있다.**
> **아래 §0~§6이 최신 결론이다. 본문의 개별 블록과 충돌하면 이 요약이 우선한다.**
> 특히 **§5(철회·정정)를 먼저 읽어라** — 본문에 옛 결론이 그대로 남아 있는 곳이 있다.

## §0. 결함 계열 최종 목록 — 아홉 개

| # | 계열 | 한 줄 정의 |
|---|---|---|
| 1 | **F18-P형** | 시나리오의 **동반 부하가 자기 감별자를 켠다** — 주입과 무관하게 실격된다 |
| 2 | **F19형** | **대조군이 주입된 구성요소를 공유한다** — 주입이 성공할수록 대조 팔이 오염돼 실격된다 |
| 3 | **H형** | **성공 신호가 주입에 고유하지 않다** — 원인이 다른 시나리오들이 같은 조건으로 통과한다 |
| 4 | **F20-R형** | **도달 불가 임계** — 신호는 오르는데 임계가 실측 상한 밖이다 |
| 5 | **J형** | **주입 산출물의 수명이 판정 창보다 짧다** — 판정 전에 관측 기반이 사라진다 |
| 6 | **인과 사슬 부재형** | 시나리오가 주장하는 **인과의 마지막 고리가 실재하지 않는다** — 신호가 아예 안 움직인다 |
| 7 | **전환 비원자성형** | **레벨 전환이 원자적이지 않다** (하위 셋, 아래) |
| 8 | **베이스라인 환경 결함형** | **주입과 무관한 이유로 환경이 상시 고장나 있다** — 감별자가 정직할수록 통과 불가가 된다 |
| 9 | **예산 소진형** | **`max_injection_duration`이 사다리 완주를 못 덮는다** — 성공 가능한 단에 닿기 전에 끝난다 |

### §0-1. 전환 비원자성형(7)의 하위 셋

| | 형태 | 사인 | 사례 |
|---|---|---|---|
| **L-1** | cleanup→apply 공백에 판정 틱이 겹침 | `*_observation_unavailable` | F20-R · F03-P |
| **L-2** | 종료 검증이 즉시 단정(폴링 없음) | `cleanup_failed` → dirty | F25-H |
| **L-3** | **전환 후 이전 파드가 정리되지 않고 공존** | **없음 — 런이 조용히 계속 돈다** | F09-P · (F12-H 부분) |

> **L-3이 가장 위험하다.** L-1·L-2는 런이 죽어 눈에 띄지만 L-3은 **오염된 실측을 남긴 채 통과·실패한다.**
> **`level_changes`가 정상 기록되므로 기존 전환 서명으로 잡히지 않는다.**

### §0-2. 관측 결함 §M-2(APM p95)의 하위 둘
**공통**: `prometheus.apm_service_p95`가 "연속 N틱" 계약과 맞지 않는다.

| | 형태 | 표본 | 게이지 대 트레이스 | 사례 |
|---|---|---|---|---|
| **M-2a** | **표본 부족형** | 분당 4~42 | **2~3배 어긋남** | F09-R |
| **M-2b** | **케이던스·비지속형**(`update_interval 60s` × 15초 틱) | 분당 343~993 (충분) | 대체로 수렴 | F09-P · F10-H |

**영향**: 사용 19종, **300~500ms 대 8종이 위험군**(임계가 게이지 잡음과 같은 자릿수).

### §0-3. 노드 사용률 게이지 (별건, 확정)
> **`kcm.node.*_utilization` 중 `system_` 접두가 없는 것은 신뢰할 수 없다.**
> `system_` 버전은 `usage/capacity`와 소수점까지 일치하며 참값이다. 해당 게이지는 **CPU·메모리 둘뿐**이고 CPU는 수리됨.
> **방향이 노드마다 다르다** — tb-w1 과소(55 대 94), tb-w2 과대(60 대 48). **보정 불가, 교체만이 답.**

## §1. 시나리오별 현재 판정

| 시나리오 | 계열 | 이번 주기 | 선행 조건 / 비고 |
|---|---|---|---|
| **F18-P** | F18-P형 + 아웃박스 교차배수 | **가능** | §B → §A |
| **F19-P·F19-S** | F19형 | **가능** | 베이스라인 감별자 제거 |
| **F03-H** | **F19형(오늘) + 예산 소진형(과거 2런)** ← 두 계열 | **가능** | 감별자 제거 + 예산 35m↑ |
| **F03-P** | **인과 사슬 부재형** (+ L-1) | **불가 → parked** | payment 지연이 checkout 5xx로 안 이어진다 |
| **F09-R** | **인과 사슬 부재형** ← 재분류 | **불가 → parked** | 노드 CPU 99.8%에서도 앱 지연이 안 오른다 |
| **F05-P** | **게이지 blind 단독** ← 재분류 | **가능(한 줄)** | §M-1 게이지 교체 |
| **F15-P** | 주입 강도 부족(근본) + 게이지(직접) | 조건부 | **§M-1 선행 필수** |
| **F20-R** | F20-R형 + J형 + L-1 | 조건부 | 임계 재보정 + 사다리 rps |
| **F15-T1** | success/abort가 같은 신호 같은 방향 | **가능** | abort 예산 상향 + min_hold 단축 |
| **F11-R** | 감별자가 성공의 부수 효과를 실격 (간헐, 5승 1패) | **가능** | 지표 교체 |
| **F25-H** | L-2 + 주입 강도 부족 | 조건부 | R1(폴링) 선행 |
| **F10-H** | **베이스라인 환경 결함형** | **불가 → parked** | courier 풀 포화 수리 선행 |
| **F09-H** | **예산 소진형** | **가능(한 줄)** | 예산 30m → 40m↑ |
| **F15-R** | J형(**이미 수리됨**) + 예산(조건부·미발현) | 관찰 | min_hold는 못 줄인다(에피소드 산술) |
| **F09-P** | **M-2b + L-3** | 조건부 | **R1 선행** — L2 실측 오염 |
| **F10-P** | **판정 유보**(사전등록 규칙 4) | — | 증거는 주입 원인 쪽. §B 근거로 못 쓴다 |
| **F12-H** | (통과 확인) | — | success 구간 주입 파드 단독 — 깨끗하다 |
| **F04-H** | (통과 확인, **4연속**) | — | **§A "commerce는 안전"을 강화.** §L-3 검사 통과 |
| **F15-H** | **§F 계열 B + §M-2b** (0승 3패) | **가능** | 감별자 하한 + 케이던스 둘 다 |
| **F15-T2** | **계열 밖 — 러너 인프라 결함** | 러너 수리 | apply 242초 → 워치독 → 펜싱 거부. **틱 0개** |

### ★ F15-T2 — **아홉 계열 어디에도 안 들어간다. 판정을 시작조차 못 했다.**

`F15-T2-run-344e8546`, `dirty / cleanup_failed after runner_exception:RuntimeError`, **`ticks.jsonl` 자체가 없다.**
```
[ERROR] Adaptive controller failed closed: operation rejected by fencing token
cleanup.reason: "timeline.compose:CalledProcessError:profile control refused:
                 operation rejected by runner lease/fencing proof; load.north_south:(동일)"
lease: fencing_token 347   /   현재 coordinator: next_fencing_token 348, active_lease null
```

#### 시간 사슬 (확정)
```
00:54:31  companion load.north_south apply        → 00:54:32 완료 (1초)
00:54:32  timeline.compose apply                  → 00:58:34 완료  ★ 242초
00:55:55  시터 poll error: timed out                     ← 그 사이 러너 API 무응답
00:57:25  시터 poll error: timed out                     ←
00:58:39  watchdog-cleanup 시작 (timeline.compose)
00:58:43  watchdog-cleanup (load.north_south)
00:58:49  cleanup 실패 — 펜싱 거부 → dirty
00:59:34  시터가 DIRTY 감지 → skip
```

#### 해석 (가설, 강함)
**`timeline.compose` apply가 242초 동안 러너를 붙잡았고**(다른 프로파일 apply는 1초),
그 사이 API가 무응답이라 시터 폴링이 두 번 타임아웃했으며,
**워치독이 발동했을 때는 리스가 이미 밀려나(347 → 348) 자기 cleanup조차 거부됐다.**

**확정된 것**: apply 242초 / 폴링 2회 타임아웃 / 워치독 발동 / 펜싱 거부 / 틱 0개.
**추론**: 긴 apply가 무응답과 워치독 발동의 원인이다. **누가 348을 가져갔는지는 확인 못 했다.**

#### 왜 중요한가
- **판정 결함이 아니다.** F15-T2는 관측을 한 번도 못 했다 — 오늘 아홉 계열과 층이 다르다
- **`timeline.compose`를 쓰는 다른 시나리오도 같은 위험**을 진다. **F15-T1이 같은 프로파일**이다(오늘 스킵됨)
- **자기 cleanup이 거부되면 dirty로 남는다** — 시터 액션을 소비하고 클러스터에 잔여 효과가 남을 수 있다.
  이번엔 워치독 cleanup이 apply 직후라 잔여가 적었을 것으로 보이나 **확인 안 했다**

#### 수리 방향 (미확정)
- **apply에 타임아웃·비동기화**가 필요해 보인다 — 242초 동안 러너가 잡히면 안 된다
- **리스 갱신이 긴 apply 중에도 이어져야 한다.** 지금은 apply가 길면 자기 리스를 잃는다
- **워치독과 펜싱의 상호작용** — 워치독이 살리려고 부른 cleanup이 펜싱에 막히면 회복 경로가 없다

**러너 소스 조사 필요. 이번 배치 범위 밖이다.**

### ★ F15-H 판독 (2026-08-08 00:55) — **예측 절반만 맞음**

> ⚠ **이 예측은 사전 등록이 아니다.** 런 `t1`이 00:40:37Z인데 예측을 00:44~45경에 보냈다 — **런 진행 중 등록**이다.
> F10-P·F15-P는 커밋 시각이 `t1`보다 앞선 것을 실물로 확인했지만 **이번엔 그 구속력이 없다.**
> (F15-H 데이터를 보지 않고 F06-P 실측만으로 예측한 것은 사실이나, "측정 전 등록"이라 부를 수는 없다.)

`F15-H-run-d718990e`, `aborted / must_rule_out_detected`, 레벨 0, **streaks: mro 2 · success 0**.

**맞은 부분** — `achieved_rps` **29/29 틱 전부 15 미만**(min 2.18 max 5.07)이고
`must_rule_out`에 `load-not-delivered`(`achieved_rps < 15`)가 있다. **설계 부하 5rps에 하한 15 — F06-P와 같은 구조다.**

**틀린 부분** — F06-P는 success streak이 **16**까지 쌓였는데 **F15-H는 0**이다.
"success 증거가 쌓여도 확정을 막다가"라는 내 서술은 여기서 성립하지 않는다. **success는 애초에 못 섰다.**

#### 왜 success가 0인가 — **§M-2b가 여기서도 작동한다**

`success.all` 3조건 (ct=3):

| 조건 | 실측 | 충족 |
|---|---|---|
| `commerce_blocked_db_sessions >= 1` | 10~12 | **29/29** ✓ |
| `food_create_429_rate >= …` | 0.79~1.00 | ✓ |
| **`commerce_order_p95 >= 2000`** | min 2.6 / max **10,626** | **9/18** — 그리고 **29틱 중 11틱이 unusable** |

**도달 불가 임계가 아니다** — 10,626까지 오른다. **문제는 지속이다.**
`commerce_order_p95`는 60초 케이던스 게이지라 29틱 중 18틱만 값이 있고, 그중 9틱만 충족한다.
**성립·불성립·unusable이 뒤섞여 3연속이 서지 않는다.** §M-2b(케이던스·비지속형) 그대로다.

**→ F15-H는 결함이 둘 겹쳐 있다: §F 계열 B(감별자 하한 > 설계 부하) + §M-2b(케이던스).**
감별자만 고쳐도 success는 여전히 3연속을 못 채운다. **둘 다 고쳐야 한다.**

#### 부수 관찰 — 대조 팔이 관측되는데 게이트에 안 쓰인다
`food_create_429_rate_baseline`이 **0.75~1.00**인데(주입 팔 0.79~1.00과 거의 같다)
`must_rule_out`에는 **베이스라인 조건이 없다**(`load-not-delivered`·`food-order-pod-down`·`food-promoted-to-5xx`뿐).
**대조 팔을 재기만 하고 판정에 안 쓰는 형태** — 이전 사이클에서 세운 "대조군을 측정하되 게이트하지 않는다" 범주다.
여기서는 다행이다(§F19형 오발화를 피했다). 다만 **의도된 것인지 확인이 필요하다.**

이력 **0승 3패**(08-03·08-07·08-08 전부 aborted).

**§G 통과 감사 결론: 거짓 통과 0건. 파드 공존 재검증 후에도 유지되고, F04-H 추가 후에도 유지된다.**

### ★ F04-H 검증 (2026-08-08 00:40) — §A 강화 + §L-3 세 번째 재현

`F04-H-run-96501345` (t1 00:25:59Z, t2 00:35:28Z, **단일 레벨**, 승격 0회).

**축적이 완전 단조 증가한다** (evaluating 23틱, unusable 0/23):
```
el=53   6  →  el=91  143  →  el=203 1,303  →  el=335 2,743  →  el=466 4,244
감소 지점 0개. success(> 20, ct=3)가 el=128에 확정되고 최종 streak 22.
must_rule_out 4개 전부 미발화 — 특히 shipping_consumer_lag 전 틱 0(소비자가 굶는다는 방향 증거).
```
cleanup 후 5,144 → … → 0으로 전량 배수 — 정답지의 *"손실이 아니라 지연"*과 일치한다.

#### ⚠ 앞선 "3009/2564/2290"은 **창 규칙 미적용 집계였다** (정정)
`succeeded` phase 틱까지 세어 과대 집계됐다. **evaluating 기준 재측정:**

| run | 소요 | **max** | **축적률/분** |
|---|---|---|---|
| 5a2b11cd (08-03) | 562s | 1,598 | **234** |
| 5885ce3f (08-06) | 566s | 2,496 | **363** |
| dfdb02c0 (08-07) | 565s | 2,290 | **333** |
| **96501345 (08-08)** | **569s** | **4,244** | **616** |

소요·틱 수가 네 런 모두 거의 같으므로(562~569초, 23틱) 차이는 축적률이고 **증가 추세다.**
→ **"감소하다 임계 아래로 내려간다"는 우려는 기우다.** 임계 20 대비 실측 4,244로 **212배 여유.**

#### §L-3 검사 통과 + **기전 세 번째 재현 (이번엔 축적으로)**
```
00:26:00~00:27:00  2j469, snbjx 공존 (75초)   outbox   6 → 8      ← baseline 파드가 계속 배수
00:27:15~00:33:45  snbjx 단독 (390초)         outbox 143 → 4,244  ← ★ 공존 종료 후 축적 시작
                   ↑ success streak 1이 el=91 = 00:27:30
```
**success 성립 구간 전체가 주입 파드 단독이다.**
그리고 **공존 중에는 미발행이 거의 안 쌓였다** — baseline 파드가 살아 릴레이를 계속 돌렸기 때문이다.

→ **F09-P(추론) → F12-H(지연 폭증) → F04-H(축적 개시)로 세 번째 재현이고,
이번엔 지연이 아니라 축적으로 나타났다. §L-3 기전이 신호 종류와 무관하게 성립한다.**

## §2. 수리 순서 (의존성)

```
1. §M-1 게이지 교체                         ← 최우선, 한 줄
       └→ F15-P 재실행 가능 / F05-P 살아남
2. §B 부하 계약 (surge.js:82 등 3파일)
       └→ F18-P·F15-P 판정 가능 (안 고치면 무엇을 고쳐도 중단)
3. §A ledger 릴레이 정지
       └→ F18-P success 성립 가능
4. R1 실행기 recovery 폴링 30초 + 이전 파드 잔존 기록
       └→ F09-P 임계 재보정 가능 / F25-H dirty 방지
5. 예산 상향 (F03-H 35m↑, F09-H 40m↑)      ← 독립, 한 줄씩
6. 감별자 제거 (F19-P·F19-S·F03-H)          ← 독립
7. 임계 재보정                               ← 1·2·4 완료 후에만 의미 있음
```

**핵심 의존성 셋**: 게이지 → F15-P / §B → F18-P·F15-P 판정 / R1 → F09-P 임계

## §3. 임계 재보정 제약 넷

1. **사다리 파라미터 버전을 병기한다**
2. **사다리를 바꾸면 그 시나리오의 과거 실측은 전부 무효다** — `32c4308`이 F05-P·F25-H·F03-H를 동시에 바꿨다(08-07 07:52Z 이전 실측 사용 불가)
3. **강도와 임계를 같은 주기에 함께 바꾸지 않는다**
4. **"그 구간에 주입이 실제로 걸렸다"는 독립 증거를 병기한다** — F09-P L2가 그 필요를 보여줬다

## §4. 조사 규칙

**두 창 규칙** — 오늘 네 번, 08-03·08-06에도 발생.
- **시간 경계**: BEFORE는 **직전 런 `t2` 이후**. UTC 통일 (**런 디렉터리 mtime은 KST**)
- **상태 경계**: 원인 판단은 **`evaluating` 틱만**. `aborted`/`recovery`/`dirty`는 **결과이지 원인이 아니다**

**대조군 규칙** — 음성 결과엔 **양성이어야 할 대조군을 같은 검색에 넣어 검증한다.** 오늘 집계 오류 둘을 잡았다.

**레지스트리** — 셋 다 **id를 키로 쓰는 dict**. **`scenario-metadata.json`의 `injected_fault`가 `profiles.json`과 9종에서 어긋난다** — 집계는 `profiles.json` 기준.

## §5. 철회·정정된 결론

| 옛 결론 | 현재 | 근거 |
|---|---|---|
| **§D-2 "승격이 관측 프로브를 끊는다"** | **철회** | 승격 성공 6건 무사, 관측 불가 3건은 전부 승격 0회 |
| §M-2 = "표본 부족" | **하위 둘로 확장** | F09-P가 표본 충분한데 같은 증상 |
| **F05-P = parked 권고** | **한 줄 수리로 가능** | 게이지 교체 시 93.74 > 임계 92 |
| **F09-R = F20-R형** | **인과 사슬 부재형 → parked** | 임계를 내려도 안 된다 |
| **F15-R = 잠재 위험 1위** | **위험도 낮춤** | 5런 전부 승격 0회 |
| F03-H = F19형 단독 | **두 계열** | 과거 2런이 `maximum_injection_duration` |
| north_south **41종** | **50종** | `profiles.json` 기준 |
| "최상위 단이 구조적으로 오염된다" | **반증** | F12-H 최상위 공존 105초로 정상 정리 |
| "`mem_utilization`은 파드 requests" | **반증** | request/capacity는 상수인데 게이지는 변동 |
| "APM 톱니파가 F09-R 괴리 원인" | **반증** | `max_over_time` 붕괴가 이미 적용됨 |
| F10-P "거짓 양성일 것" | **지지받지 못함** | account 144배가 주입 원인을 가리킨다 |
| F15-P "blind 게이지 때문에 실패" | **반증(제3의 결말)** | 참값도 75 미만 — 강도 부족 |

## §6. 미규명

**규명되면 수리안이 바뀌는 것**
1. **F09-P 7월→8월 전환** — 레벨 0 동일한데 7월 3승(183초) → 8월 2패(27분). **임계 조정보다 먼저**
2. **F09-P L2 파드 공존이 510초였던 이유** — F12-H는 같은 최상위 단에서 105초
3. **courier 풀 포화의 원인·규모** (F10-H)

**분류·해석에 남은 것**
4. F10-P `account` 선택적 회복 (L1에서 account만 1,877→672, transfer는 계속 아프다)
5. `kcm.node.mem_utilization`의 정확한 정의 (판별 규칙은 섰으므로 실익 낮음)
6. env 계열의 파드 공존 판별법 (cpu_limit 동일해 구분 불가)
7. F15-R `episode-fault-300`이 존재하는 이유
8. F09-P L2 두 파드 간 트래픽 분배 비율 (트레이스 `k8s_pod_name` 비어 있음)
8-b. **F04-H 축적률이 234 → 616/분으로 는 원인** — 재고 수리(`52f23b6`, 08-07 08:35)만으로는 설명 안 된다
   (그 이후인 dfdb02c0가 333인데 이번이 616). 지금은 임계 대비 212배 여유라 무해하지만,
   **무엇이 commerce 주문 생성률을 바꾸는지는 다른 시나리오에도 걸린다**

**설계 질문**
9. **사다리 첫 단 무효 관행** — F03-H·F25-H·F20-R에서 반복
10. **`achieved_rps` 감별자 두 계열** — `gt`는 건당 비용에 눈멀고 `lt`는 과민. 29건/26종

---

## B. `load.north_south` 무제한 목록 조회 — **선행 조건**

이걸 안 고치면 F18-P·F10-P는 무엇을 고쳐도 판정에 도달하지 못한다.

**기전 (확정)**
`load.north_south`가 `GET /api/transfers`를 page/size 없이 호출 →
`TransferController.java:60-63`이 **의도적으로** `Pageable.unpaged()` 사용 (주석에 근거 명시) →
`banking.transfers` 980,498행 전수 조회 → Hikari 풀(15) 고갈 →
동기 이체 POST가 502 → `transfer_2xx_rate` 0.44 → 감별자 `sync-path-also-broken`(`lt 0.95`) 발화 → 중단.

주입과 **인과관계 없음**. 시나리오가 자기 동반 부하로 자기 감별자를 켰다.

| | 실측 (F18-P 주입 창) |
|---|---|
| `POST /api/transfers` | 200: 733 / 400: 18 / **502: 915** → 2xx ≈ 0.44 |
| `GET /api/transfers` | 200: 675 / **502: 3459** |
| `GET /api/accounts*` | 거의 전량 200 (다른 서비스·다른 풀) |
| 주입 없는 평시 최근 5분 | **502 = 0건** |

영구 파손이 아니라 부하량 의존 조건부 파손이다.

**수리 방향**: 앱이 아니라 **부하 계약** 쪽. 무제한 조회는 앱의 의도된 동작이고 주석에 근거가 있다.
엔드포인트에 상한을 두면 F17-R 등 다른 시나리오의 전제를 건드린다.

### 수리 표면 — 41종이 아니라 **한 줄 × 3파일** (확정)

`load.north_south`를 쓰는 시나리오는 41종이지만(companion 38 + 주입 자체 3),
**무제한 목록 조회는 banking 스크립트에만, 각 한 줄씩 있다.** k6 스크립트 9종 전수 확인:

```
/opt/loadgen/core-banking/surge.js:82                 ← F10-P, F15-P, F18-P
/opt/loadgen/core-banking/transfer-heavy-surge.js:82  ← F21-P (parked)
/opt/loadgen/core-banking/script.js:64                ← 기저 부하 (항상 도는 것, TARGET_RPS=1)
```
```js
const res = http.get(`${GATEWAY_URL}/api/transfers?fromAccount=${id}`, {   // page/size 없음
```

**같은 파일의 나머지 호출은 전부 유계다** — `/api/accounts?status=ACTIVE&size=20`(:89), 단건 조회, POST.
commerce·food surge 계열에는 무제한 목록 조회가 **없다**(전부 `size=20`).
**banking 이체 목록만 관행에서 빠져 있었다. 설계 실수 한 줄이다.**

→ **`&page=0&size=20` 추가.** 새 값을 고르는 게 아니라 이 테스트베드가 이미 쓰는 값을 맞추는 것이다
(commerce products, food restaurants/deliveries, banking accounts 전부 20).
호스트: tb-runner `nkia@192.168.122.206`, `/opt/loadgen/` 아래.

### 실측 근거

```
GET /api/transfers?fromAccount=ACC-1001                → 10,518,310 B (10.0 MiB), 49,266행, 0.258s
GET /api/transfers?fromAccount=ACC-1001&page=0&size=20 →      4,089 B,            20행, 0.066s
GET /api/accounts?status=ACTIVE&size=20  (대조군)       →      1,618 B,                  0.004s
```

**2,572배.** 30 rps × 10 MiB ≈ 300 MiB/s 직렬화, transfer 힙은 `-Xmx512m`. 풀 고갈 + GC 압박을 함께 만든다.

`banking.transfers` 980,970행의 계정별 분포 — loadgen이 쓰는 ACC-1001~1007이 **각 약 5만 행**:
```
commerce-settlement 618,607 (loadgen 미사용) / ACC-1005 50,345 / ACC-1002 49,687 / ACC-1007 49,329
ACC-1001 49,266 / ACC-1006 49,263 / ACC-1004 47,298 / ACC-1003 47,118
```
**이체가 계속 쌓이므로 매일 커진다** — 오늘 10MB면 다음 주엔 더 크다. 시간이 갈수록 나빠지는 형태다(→ 보존 정책).

### 건드리면 안 되는 것 (확정)

**F20-Q "food 주문 목록의 페이지네이션 부재 — 무제한 결과셋이 힙을 채운다"** — 페이지네이션 부재가 **주입 자체**다.
다행히 별개 스크립트(`food-delivery/slowquery.js:94` `/api/orders`)의 별개 엔드포인트라 banking 수리와 겹치지 않는다.
F20-P("trunc() 풀스캔")도 `/api/transfers/stats/daily?days=90` — 통계 엔드포인트라 무관.

### 임계 조정 — 불필요 (확정)

north_south 사용 시나리오의 부하 임계는 전부 `achieved_rps`(초당 요청 수)이고 **응답 크기와 무관**하다.
```
F10-P·F15-P  achieved_rps > 60 (target 20)   F17-P  > 40 (frozen-bypass, 무관)
F18-P        transfer_2xx_rate < 0.95 (target 30)
```
※ 가설: 응답이 작아지면 k6가 목표 rps를 더 쉽게 달성해 `achieved_rps`가 **오른다**.
임계 60은 target 20~30 대비 여유가 크지만 수리 후 첫 실행에서 실측 확인할 것.
반증법: 수리 후 F18-P를 동일 target_rps로 1회 실행해 대조.

### 다이제스트 — 해싱하지 않는다. 세 줄만 고치면 끝이다. (확정)

- `_digest()`는 **JSON 정규화 해시**이고 파일을 읽지 않는다 (`contracts/compile-plan.py:29,33,421,429`)
- compile-plan.py가 파일을 읽는 곳은 둘뿐: JSON 레지스트리(`:21`), 실행기 `.py`(`:385 executor_sha256`)
- `grep -n "script_path" compile-plan.py` → **0건**. 경로는 parameters 값으로 실려 다닐 뿐 열지 않는다
- 캡슐 `hashes.json` 425개 파일 중 **`.js` 0개** — k6는 계약 루트 밖(tb-runner `/opt/loadgen/`)
- 실행기도 내용 미검증: 경로 allowlist(`:49`)와 `-r` 확인(`:137`)뿐
- `execution.py:53 script_sha256`은 **레거시 시나리오 실행 스크립트**(`runner.py:171`)이고 k6와 무관

→ **레지스트리 변경·매니페스트 재생성 불필요. 기존 런 다이제스트와도 어긋나지 않는다.**

> ⚠ **실행기 `.py`는 건드리지 마라.** `load_north_south_executor.py`를 고치면 `executor_sha256`이 바뀌고
> 그건 `plan_digest`에 들어간다. `.js`만 만지는 한 무관하지만, "겸사겸사" 실행기까지 손대면 재생성이 필요해진다.

### 별도 확인 대상
F15-P는 `transfer_2xx_rate`를 관측하지 않는다(p95·노드 자원만). 무제한 조회는 F15-P에서
**`transfer_p95`·`account_p95`를 부풀리는** 형태로 오염된다 — 중단은 안 시키지만 지연 신호를 못 믿게 만든다.
반증법: 수리 전후 `transfer_p95` 대조. (미조사)

**타이밍 함정 (확정)**: F18-P에서 2xx는 주입 **45초 뒤부터** 무너져 24틱 내내 임계 아래였다.
`min_hold` 8분이 그걸 가리다가 만료 직후 2틱으로 집행했다. **중단은 처음부터 예약돼 있었다.**
→ 로그에서 "min_hold 만료 직후 중단"을 보면 위배 시작 시각을 반드시 거슬러 확인할 것.

---

## A. banking 아웃박스 교차 배수 — ledger 릴레이 정지

**기전 (확정, 트레이스 전수)**
transfer·ledger **둘 다** `outbox.relay.enabled: true`이고 **둘 다 `default_schema` 미설정** →
같은 `BANKING.outbox_events`를 무스코프 `findTop100ByPublishedAtIsNullOrderByCreatedAtAsc()`로 경쟁 소비.

24시간 전수 (08-06 19:00 ~ 08-07 19:44):

| 발행 서비스 | `banking.transfers` | `banking.ledger` |
|---|---|---|
| core-banking-transfer | 13,072 | **13,154** |
| core-banking-ledger | **13,696** | 13,074 |

**양방향이다.** 행 하나가 어느 릴레이에 집히는지는 동전 던지기.

F18-P 주입 창 자연 실험:

| 구간 | transfer | ledger |
|---|---|---|
| 주입 전 | 10 | 4 |
| **주입 중** | **0** | **110** |
| 주입 후 | 19 | 12 |

→ **주입은 자기 대상에 완벽히 작동했다.** ledger가 빈자리를 흡수해 흔적을 지웠을 뿐.
스팬 속성 `thread.name=scheduling-1`(= `@Scheduled relay()` 전용)이 못을 박는다.
이 하나로 08-03부터의 F18-P 5회 실패 **전부**가 설명된다.

**세 도메인 대조 — banking만 격리 장치가 없다**

| 도메인 | relay 켠 서비스 | 격리 | 결과 |
|---|---|---|---|
| commerce | 5종 | 서비스별 PG 스키마 (`hibernate.default_schema`) | 안전 — F04-H 3/3 성공 (미발행 2290~3009 축적) |
| food-delivery | 3종 | 서비스별 테이블 (`*_outbox_events`, 제네릭 릴레이) | 안전 |
| **core-banking** | 2종 | **없음** (`default_schema` grep 0건) | **F18-P 5/5 실패** |

commerce는 서비스 5종·제어 필드 부재라는 **더 나쁜 조건**에서도 한 줄로 격리가 성립한다.

**이번 주기 수리: `ledger-service/application.yml`의 `outbox.relay.enabled: false`**

손실 0이 확정됐다:
- 전 도메인 `@KafkaListener` 8개 전수 — `banking.ledger` 구독 **0개**
- Kafka `--all-groups` — `banking.ledger` 컨슈머 그룹 **부재**(오프셋 커밋 이력조차 없음)
- 토픽에 약 **862,565건**이 쌓인 채 소비된 적 없음
- `LedgerEventPublisher.java:14-15` 주석이 스스로 인정 ("현재 consumer 없음, 향후 확장 지점")

끈 뒤 동작:
- ledger의 `OutboxRelay`·`OutboxRelayStore` 빈 소멸 (둘 다 `@ConditionalOnProperty`) → 교차 배수 소멸
- ledger의 LEDGER 행 INSERT는 **계속**됨 (`OutboxPublisher`는 무조건 `@Component`)
- 그 행의 발행은 **transfer 릴레이가 수행** — 이미 13,154건 하고 있다는 직접 증거
- F18-P 주입 시 = 유일한 배수구 정지 → 미발행 단조 증가 → success 조건 `> 20` 성립

**⚠ 이건 결함 제거가 아니라 발현 봉쇄다.**
무스코프 `findPending`은 그대로 남는다. 세 번째 relay-enabled banking 서비스가 생기거나
누가 이 줄을 되켜면 **조용히 재발**하고, 재발해도 오류가 안 난다 — 5회 놓친 그대로다.
- 그 줄에 이유를 주석으로 남길 것 (F18-P + 이 문서 참조)
- 테스트로 고정: banking에서 `outbox.relay.enabled: true`인 서비스가 1개를 넘지 않는다

---

## C. F18-P 레지스트리 자기모순

`scenario-metadata.json`의 F18-P에서 `cause`·`root_cause.mechanism`·`root_cause.infra_anchor`는
아직 **env 토글**(`OUTBOX_RELAY_ENABLED=false` + `@ConditionalOnProperty`, "k8s.env executor")로 서술돼 있는데,
같은 문서의 `injection_summary`·`injected_fault`는 **app.control DB 행**이고 "env 토글은 쓰지 않는다"고 명시한다.
`infra_anchor`가 `injection_summary`를 정면으로 반박한다. → 4면 동기화 대상.

---

## D. 러너 — 프로브 파일이 판정 도중 사라진다 **(승격 시에도. 심각도 상향)**

`/tmp/rca-scenario-<id>-live.json`이 사라져 관측이 통째로 unusable이 되는 결함.
**두 경로가 확인됐고, 둘째가 훨씬 나쁘다.**

### D-1. companion cleanup (런 끝, 1회) — F18-P
중단 후 5틱에서 `entry_status`·`transfer_2xx_rate`가 전부 unusable:
`exit 1: cat: /tmp/rca-scenario-F18-P-live.json: No such file or directory`.
이번엔 recovery 조건이 그 둘을 안 봐서 통과했을 뿐이다.
첫 aborted 틱의 `pod_ready=False`도 이 구간 것이라 "회복 실패"로 오독될 수 있다.

### D-2. ~~레벨 승격이 프로브 파일을 지운다~~ — **반증됨. 이 항목은 철회한다.**

> **2026-08-07 20:20 정정.** 나는 `F20-R-run-0c92dfd2`의 중단 틱에서
> `cat: /tmp/rca-scenario-F20-R-live.json: No such file or directory`를 읽고 이것을 중단의 **원인**으로 확정했다.
> **틀렸다. 그 틱은 `aborted` phase의 것 — 중단이 결정되고 cleanup이 돈 *뒤*의 관측이다.** 인과를 뒤집어 읽었다.

반증 근거 (확정):

| | evaluating 틱 | 그 중 `entry_status` unusable |
|---|---|---|
| F20-R-run-0c92dfd2 | 30 | **0** |
| F20-R-run-799ef4a8 | 30 | **0** |
| F03-P-run-27f6eee2 | 18 | **0** |

**평가 구간 내내 멀쩡했다.** `live.json` 부재는 `aborted` phase 틱에서만 나타난다.

승격 대 결과 전수 (이번 배치 사다리 런):

| 승격 | 런 | 관측 불가로 죽음 |
|---|---|---|
| **1~2회 (성공)** | F12-H, F03-P-5edd6309, F09-R, F05-P, **F11-R**, F15-T1 — 6건 | **0건** |
| **0회** | F20-R×2, F03-P-27f6eee2 — 3건 | **3건 전부** |

**정확히 반대였다. 승격은 무해하다. 승격하지 못한 런이 죽었다.**
특히 F11-R은 `load.north_south`가 **사다리 소유자**인데 승격 1회를 정상적으로 겪고 관측이 살아 있는 상태에서 판정됐다.
k6 교체가 관측을 끊는다면 여기서 끊겼어야 한다.

→ **D는 D-1(companion cleanup, 런 끝 1회)만 남는다.** 심각도 상향도 철회한다.

---

## J. **부하 수명 소진형** — 다섯째 결함 계열 (확정)

> 주입의 수명이 판정의 수명과 같거나 짧아, 판정이 결론에 도달하기 전에 **관측 기반 자체가 사라진다.**
> 사인은 `*_observation_unavailable`로 나타나 관측 결함처럼 보이지만,
> 실제로는 **주입 수명과 판정 수명의 계약 불일치**다.

### F20-R 두 런 — k6 수명 == 레벨 timeout (확정)

| 런 | k6 총 수명 | 레벨 timeout | 실제 경과 | 사유 |
|---|---|---|---|---|
| 0c92dfd2 | **480s** | **480s** | 494s | `decision_observation_unavailable` |
| 799ef4a8 | **480s** | **480s** | 493s | `safety_observation_unavailable` |

k6 수명 = `ramp_up 1m + hold 6m + ramp_down 1m` = **정확히 480초**. 여유가 0이다.
마지막 evaluating 틱이 476s, 중단이 493~494s — **k6가 스스로 끝나며 `live.json`을 남기지 않았고 러너는 아직 판정 중이었다.**

대조:

| 런 | k6 수명 | 경과 | 관측 불가? |
|---|---|---|---|
| F20-R ×2 | 480s | 493~494s | **예 — 수명 초과** |
| F07-H | 660s | 392s | 아니오 |
| F03-P-5edd6309 | 660s | 1174s | 아니오 (승격으로 수명 리셋) |
| F11-R | 660s | 782s | 아니오 (승격으로 리셋) |

**승격에 성공하면 k6가 새로 떠 수명이 리셋된다.** F20-R은 승격을 못 해 리셋되지 않았다.

### 세 층이 하나로 이어진다
**F20-R이 승격을 못 한 이유가 §I의 층 2·3이다** — 주입이 약해(rps 1) `order_p95`가 임계 근처에서 진동해
escalate 3연속이 안 서고, `payment_p95 >= 500`은 애초에 도달 불가라 success도 못 선다.
**판정이 결론에 못 가는 동안 부하 수명이 먼저 소진된다.**

### 기존 계열과의 구별
- F18-P형은 부하가 감별자를 **켠다**. 이건 부하가 **사라진다**.
- §I(도달 불가 임계)는 판정이 영원히 안 서는 것이고, 이건 그 상태에서 **시간이 먼저 끝나는** 것이다.
  인접하지만 수리 지점이 다르다(임계 vs 수명).

### 수리
**권고: 러너가 레벨 timeout 도달 시 부하 종료를 기다리지 않고 판정을 닫는다.**
"부하가 끝났다"를 관측 실패가 아니라 **레벨 종료**로 읽는 것이다. 계약 한 곳만 고치면 사다리 20건 전부에 걸린다.
(대안인 시나리오별 `hold` 연장은 매번 어긋날 수 있다.)
러너 판정 로직 변경이라 영향이 넓다 — **B·A보다 뒤, 재실행 전에는 넣어야 한다.**
안 고치면 F20-R은 임계를 고쳐도 같은 자리에서 또 죽는다.

### 미규명 — F03-P-run-27f6eee2 (가설)
k6 수명 660초인데 393초에 중단됐다. **수명 문제가 아니다.**
`level_index=1`인데 `level_changes`엔 초기 적용 1건뿐 → **승격이 시도됐으나 기록되지 못했다.**
마지막 evaluating(302s)과 aborted 첫 틱(393s) 사이 **91초 공백**. F03-P 사다리는 `k8s.env` → 파드 롤아웃.
가설: 승격 실행(롤아웃 대기)이 91초를 먹는 동안 판정 틱이 멈췄고 그 사이 companion이 재적용되며 `live.json`이 사라졌다.
**승격 성공은 무해하고 승격 *실패*가 해롭다**는 형태라 위 반증과 충돌하지 않는다.
반증법: 러너의 승격 실행 경로가 companion을 재적용하는지, `level_index` 증가 후 `level_changes` 기록 전 실패 시 동작을 코드로 확인. (미조사)

### 관측 계약 — 사인이 원인을 가린다
`safety_observation_unavailable` 3회 + `decision_observation_unavailable`이 F20-R 6런 중 4런의 사인인데,
그 뒤에 **최소 두 개의 서로 다른 원인**이 숨어 있었다.
**중단 사인에 "어느 관측이 어떤 에러로" 불가였는지를 실어야 한다.**
지금은 `decisions.json`에 reason만 있어 매번 ticks를 파야 한다.

---

## I. F20-R — **판정 임계가 도달 불가능하다. 6/6 실패의 근본.** (확정)

정체 정정: **commerce 도메인**이고 `injected_fault.profile = null`·`companions = []`,
**부하 자체가 주입**이다(`load.north_south` 사다리 + `/opt/loadgen/commerce/slowquery.js`). food가 아니다.

### 성공 조건
```json
"success": {"all": [
  {"id": "order-latency-up",               "observation": "order_p95",   "op": "gte", "value": 1500},
  {"id": "payment-crossschema-latency-up", "observation": "payment_p95", "op": "gte", "value": 500}
], "consecutive_ticks": 3}
```

**`payment_p95 >= 500`은 역대 6회 중 한 번도 성립한 적이 없다.**
게이지 최댓값이 rps=30에서조차 **269.7 / 275.4 / 286.9 / 288.6 / 290.0 / 302.3 ms**다.
**임계가 실측 상한의 약 1.65배다. rps를 30으로 되돌려도 성공하지 못한다.**

### 이력 — 6/6 실패, 성공 0회

| run | t1 (UTC) | outcome | reason | rps | max order_p95 | max payment_p95 |
|---|---|---|---|---|---|---|
| 0328ea75 | 08-03 05:09 | aborted | `safety_observation_unavailable` | 30 | 30,010 | 290.0 |
| 04b423f5 | 08-03 05:27 | aborted | 〃 | 30 | 21,200 | 302.3 |
| c6e87a5c | 08-03 05:46 | aborted | 〃 | 30 | 22,629 | 288.6 |
| 56a859db | 08-05 00:54 | **failed** | `evaluation_level_timeout` | 30 | 23,885 | 287.0 |
| fa6749a8 | 08-07 05:07 | **failed** | 〃 | 30 | 25,773 | 269.7 |
| 0c92dfd2 | 08-07 19:54 | aborted | `decision_observation_unavailable` | **1** | 1,831 | 44.2 |

`evaluation_level_timeout` 2회가 정확히 "order는 임계를 넘었는데 payment가 안 넘어 3틱 연속이 영원히 안 서고 타임아웃"이다.
**만족 불가능한 조건이 6회 내내 서로 다른 사인으로 위장돼 나타났다.**

### 그런데 시나리오 설계 자체는 옳다 (확정)

트레이스 3구간(BEFORE는 직전 런 `t2` 이후로 절단):

| | rps=1 (오늘) | rps=30 (fa6749a8) |
|---|---|---|
| commerce-order | 55.7 → **1,701.7** → 51.4 | 126.3 → **43,348.1** → 133.5 |
| commerce-payment | 20.0 → **59.8** → 18.6 | 60.4 → **275.4** → 75.2 |
| commerce-inventory | 3.3 → **50.0** → 3.7 | 3.3 → **90.6** → 3.7 |

**공유 PostgreSQL을 통한 크로스스키마 전파가 실재한다.** payment 3~4.6배, inventory 15배, AFTER 완전 복귀.
`distinguishing_evidence`("건강한 서비스가 함께 느려진다")가 그대로 관측된다. **문제는 설계가 아니라 임계다.**

교차 확인: `pg_slow_active_queries`가 전 구간 0인 것은 관측 오류가 아니라
질의 문턱이 `now() - query_start > interval '2 seconds'`인데 rps1의 order 지연이 1.7초라 **못 넘은 것**이다.

### 층 2 — 사다리 재설계가 rps를 30 → 1/2/3으로 낮췄다
3단을 다 합쳐도 이전 고정값의 1/10이다. order조차 임계 근처(1,831 vs 1,500)에서 진동해
낮은 쪽 3틱이 잡히며 escalate가 잘못 켜졌다.

### 수리 순서 — **R1을 먼저 하지 않으면 나머지가 헛돈다**
1. **R1 `payment_p95` 임계 재보정** — 500은 근거 불명이고 실측 상한(302)을 넘는다.
   BEFORE 대비 배수는 rps30에서 4.6배·rps1에서 3.0배로 **일관되게 크다** — 신호는 분명히 있다.
   절대값보다 **상대 배수**가 이 시나리오에 맞아 보이나 컨트롤러 지원 여부는 미확인.
   절대값이면 rps30 기준 **200ms 안팎**이 후보. **⚠ 두 점(rps 1·30)뿐이므로 사다리 확정 후 실측으로 정할 것.**
2. **R2 사다리 rps 재설계** — 10/20/30 범위가 후보. R1 뒤에 두는 이유: rps를 올려도 임계 500은 못 넘는다.
3. **R3 승격 시 관측 단절** — D-2와 병합.
4. **R4 `order_p95` 게이지 분해능**(별건) — 슬로우쿼리(1200~1800ms)와 체크아웃(6~53ms)이 같은 게이지에 섞여 진동한다.
   부하 구성 40/40/20은 의도된 설계이므로 **게이지를 스텝별로 가르는 쪽**이 맞아 보인다. (미조사)

---

## 조사 중 (2026-08-07 19:45 시점)

### F19-P·F19-S — **해결. 대조군이 대조군이 아니다.** (확정)

둘 다 `aborted must_rule_out`, success 스트릭 14·16이 쌓인 상태에서 거부됐다.

**주입 대상**(`scenario-metadata.json`):
```
F19-P  mock.expectation  /pay  delay 8s   "외부 PG 지연(타임아웃 미만) — 502 없이 order 커넥션 풀만 마른다"
F19-S  mock.expectation  /pay  delay 30s  "외부 PG 지연(타임아웃 초과) — 502와 서킷 개방이 주문으로 연쇄한다"
```

**모든 주문이 `/pay`를 지난다 — 베이스라인 암도 포함해서.**
따라서 `order_create_5xx_rate_baseline = 1.0`은 주입이 **작동한** 증거이지 교란 요인의 증거가 아니다.
**베이스라인 암은 이 시나리오의 대조군이 될 수 없다. 주입된 구성요소를 공유하기 때문이다.**

`7c90e08`이 넣은 베이스라인 감별자는 **정확히 주입이 성공할 때 거부한다.** 구조적으로 무효다.

#### 트레이스 실측 — 환경은 멀쩡하다 (`POST /api/orders`, food-delivery-order SERVER)

| 구간 | 5xx | 전체 | 비율 |
|---|---|---|---|
| 공백 (F17-R 후, 18:59–19:04) | 0 | 6 | 0 |
| F18-P 주입 (banking, 19:04–19:14) | 0 | 11 | 0 |
| 공백 | 0 | 4 | 0 |
| **F19-P 주입 (19:18–19:27)** | **182** | **223** | **0.816** |
| 공백 | 0 | 4 | 0 |
| **F19-S 주입 (19:32–19:42)** | **229** | **242** | **0.946** |
| 공백 | 0 | 4 | 0 |
| F16-H 주입 (commerce, 19:46–19:54) | 0 | 14 | 0 |
| F20-R 이후 | 0 | 16 | 0 |

**5xx는 F19 주입 창에만 있다. 나머지 전 구간 59건 중 5xx 0건.**
분모도 223·242로 크다 — 표본 부족이 아니다.

#### 그래서 무슨 일이 있었나 — 오늘 수리 둘의 상호작용
1. **이전**: `dispatches` status 인덱스 부재 → 주문 생성마다 풀스캔 → 상시 5xx →
   F19가 **환경 5xx로 거짓 통과**했다. (이게 `7c90e08`을 넣은 원래 이유다)
2. **오늘 수리 ①**: `(status, assigned_at)` 인덱스 투입 → **환경이 깨끗해졌다**(위 표가 증거).
3. **오늘 수리 ②**: 베이스라인 감별자 추가 — 2차 방어선으로 넣었다.
4. **그런데 ②는 구조적으로 무효다.** 베이스라인이 주입된 목을 공유하므로,
   ①이 원인을 제거한 지금 ②는 **유효한 런만 거부한다.**

**→ 수리: `7c90e08`의 베이스라인 감별자를 F19-P·F19-S에서 제거한다.**
원래 막으려던 환경 5xx는 인덱스 수리로 이미 사라졌고, 그 사실은 위 표로 검증됐다.
재발 감시가 필요하면 베이스라인 암이 아니라 **주입 목의 상태**를 보는 조건이어야 한다.

> **교훈**: 대조군을 설계할 때 "주입된 구성요소를 지나지 않는가"를 먼저 확인해야 한다.
> 공유 진입점을 쓰는 부하는 대조군이 아니다. — 오늘 F18-P의 "자기 부하가 자기 감별자를 켠다"와 짝을 이루는 결함이다.

미결: `payment_error_rate=100.0` — 다른 신호는 0~1인데 이것만 100. 단위 계약 불일치 의심.

### E. 비율 신호가 분모 없이 기록된다 — **확정, 감사 불가능성의 원인**

`ticks.jsonl`의 신호 봉투는 비율만 담는다. 표본 수가 없다:

```json
{"source": "k6:baseline:food-delivery:business_5xx_rate",
 "value": 0.0, "quality": "good", "usable": true,
 "freshness": "fresh", "observed_at": "...", "update_interval_sec": 30, "error": null}
```

→ **"1.0이 2/2인지 40/40인지"를 런 산출물로 답할 수 없다.** F19 진단이 여기서 막혔다.
비율 기반 게이트 **전부**가 사후 감사 불가능하다는 뜻이다.

오늘 `loadgen_monitor.py`에서 분모(`checkout_count`·`read_count`)를 항상 발행하도록 고쳤지만,
**러너가 그것을 판정 기록으로 옮기지 않는다.** 관측 시점에는 존재하는 값이 기록에서 사라진다.

베이스라인 문서(`/app/state/loadgen/baseline-*-live.json`)는 런 중에만 존재하고 cleanup에서 사라지므로
사후 조회로도 복구 불가능하다(§D와 같은 계열 — 판정 근거가 판정보다 먼저 삭제된다).

**수리**: 비율 신호의 봉투에 표본 수를 함께 실어라(최소한 `ticks.jsonl`에).
분모 없는 비율은 판정 근거로 쓸 수 없다 — 이건 오늘 다섯 번째 "측정이 거짓말하는" 사례다.
우회로는 트레이스에서 직접 세는 것뿐이고, food-502 에이전트가 그 방법으로 진행 중이다.

---

## F. `achieved_rps` 감별자 29건/26종 — 한 지표가 두 실패 양상을 동시에 만든다

`controllers.json` + `controllers-parked.json` 전수. **두 계열이 같은 신호를 반대 방향으로 읽는다.**

### 계열 A — `gt` (부하 과다 배제형) 15건: **건당 비용 급증에 눈이 먼다** → 거짓 음성 → 거짓 성공

F10-P·F15-P·F21-P(각 `gt 60`/`gt 45`, target 20·30) / F17-P `gt 40` / F02-H·F05-P·F09-H·F09-R `gt 60` /
F09-P `gt 65`(target 60) / F10-H `gt 60` / F12-H `gt 40`(target 35) / F21-Q `gt 100` /
**F03-P `overload-not-pool gte 100`** / F05-R `general-overload gt 45` / F11-R `gt 70`(target 65)

막으려던 것: "고장이 아니라 부하가 많아서 느린 것"의 배제.
맹점: **rps는 그대로인데 요청 하나의 원가만 오르는 형태를 통과시킨다.** F10-P가 그 실례다.

특히 **F03-P의 `overload-not-pool`은 이름이 "과부하지 풀 문제가 아니다"를 가른다는 뜻인데,
정작 풀을 마르게 하는 것은 rps가 아니라 커넥션 점유 시간이다.**
감별자가 자기가 가르겠다고 선언한 축을 안 보고 있다.

### 계열 B — `lt` (부하 전달 확인형) 14건: **반대로 과민하다** → 거짓 양성 → 거짓 중단

F03-H·F03-P·F05-H·F06-H·F06-P·F07-P·F08-P·F15-H·F15-R·F15-T2·F17-R `lt 15` /
F05-R·F11-R `lt 30` / **F07-H `surge-not-achieved lt 100`(target 160)**

막으려던 것: "부하가 실제로 전달됐음"의 확인 — 로드젠이 죽은 창을 무효화한다.
맹점 아님. 다만 요청이 비싸지면 k6가 목표 rps를 못 채워 `achieved_rps`가 **떨어지고** 이게 발화한다.

**→ 건당 비용이 오르면 A는 침묵하고 B는 비명을 지른다.**

### F06-P 실증 — 기전이 완전히 드러났다 (확정, `F06-P-run-2775cd4f`)

`aborted / must_rule_out_detected`, level 0, **streaks: success 16 · must_rule_out 2**.

```
must_rule_out.any:  {"id": "load-not-delivered", "observation": "achieved_rps", "op": "lt", "value": 15}
success.all:        {"id": "order-create-rejected", "observation": "order_create_429_rate", "op": "gte", "value": 0.3}
                    consecutive_ticks: 3
```

**실측 `achieved_rps`: min 2.19 / max 5.07 — 29/29 evaluating 틱 전부 15 미만.**
설계 부하가 5rps인데 감별자 하한이 15다. **주입과 무관하게 매 틱 참이다.**

**왜 success 16이 쌓이고도 죽었는가 — 오늘 문서화한 비대칭이 그대로 작동했다:**
1. settle+min_hold 동안 **success 스트릭은 쌓이고**(16), must_rule_out 스트릭은 매 틱 리셋된다(`_drop_pending_streaks`)
2. min_hold 만료 → must_rule_out이 충전되기 시작
3. 첫 틱: mro 미확정이지만 `raw_ruleout=True`라 **`safety_pending`이 success 확정을 막는다**
4. 둘째 틱: mro 확정 → abort. **success는 조건을 16틱이나 만족하고도 한 번도 평가되지 못했다**

> 마지막 틱: `order_create_429_rate` **1.0**(임계 0.3) · `order_create_5xx_rate` 0.0 · `pod_ready` 1 · `target_health` 200.
> **주입은 완벽하게 작동했다.** 죽인 것은 설계 부하보다 높은 감별자 하한 하나다.

**독립 수렴 세 번째다** — 배포 전 판단, `achieved_rps` 전수 감사(§F 계열 B), 그리고 이 실증.
수리는 §F의 계열 B 항목과 동일하다(하한을 설계 부하 아래로 내리거나 지표를 로드젠 생존으로 교체).

### 이미 알고 있던 것과의 수렴
**F06-P·F15-H는 target 5인데 임계가 `lt 15`** — 정상 동작 중에도 조건이 성립한다.
이건 배포 전부터 대기 중이던 항목("설계 부하 5rps vs `lt 15` — 항상 발화")과 **독립적으로 같은 결론**에 도달했다.
독립 수렴이므로 신뢰도가 높다. 우선 처리 대상.

### B 수리가 이들에 미치는 영향 — 임계 조정 불필요
수리는 banking 스크립트만 건드리므로 `achieved_rps`가 오르는 것은 F10-P·F15-P·F21-P(+F18-P는 이 감별자 없음)뿐이고,
각각 `gt 60`(target 20)·`gt 45`(target 30)로 여유가 크다.
※ 실제 상승폭은 수리 후 1회 실행으로 확인할 것(가설).

---

## G. 통과 16건 인과 감사 — **거짓 통과 0건** (2026-08-07 20:03 UTC 스냅샷)

범위 `t1 >= 2026-08-07T13:42:44Z`, 25런(성공 16 = evaluation 13 + calibration 3 · 중단 7 · 실패 2).

### 결론: 16건 전부 판정이 주입 때문이다. 거짓 0 · 의심 0.

전 건 `BEFORE≈0 → DURING 급등 → AFTER 0.000` 패턴이 트레이스로 확인됐다(DURING 표본 n=277~1835).

**B 수리와 독립이다** — 16건 전부 `commerce/surge.js`를 썼고 `core-banking/surge.js` 사용은 **0건**이다(런별 `plan.json` 직접 확인).
무제한 조회에 노출된 넷 중 F18-P는 중단, F21-P는 parked, **F10-P·F15-P는 이번 배치에서 아직 미실행**이다.
core-banking 통과 4건(F01-P·F08-G·F15-G·F17-R)은 commerce 진입점을 쓰는 크로스도메인이라 banking 스크립트를 안 쓴다.

### F07-H — 배치 안에 이미 있던 음성 대조군 (확정)

**`load.north_south` 주 프로파일, rps 160(전 시나리오 최대), checkout 실패율 0.000, n=1,835.**

→ **commerce 부하는 그 자체로 checkout 5xx를 만들지 않는다.**
F18-P를 망친 "부하가 감별 신호를 만든다" 함정이 commerce 경로에는 없다는 직접 증거다.
따라서 다른 런에서 주입 창에만 5xx가 오르는 것은 주입 때문이라고 읽어도 된다.

### 감사 도구 `causality_absent` 6건은 전부 거짓 음성 (확정)

| 함정 | 대상 | 시나리오 |
|---|---|---|
| **A. 대상이 DB라 스팬이 없다** | `target_kind=database` | F06-H(PG payments), F08-G·F15-G·F01-P(Oracle accounts) |
| **B. 대상이 죽어서 스팬을 못 남긴다** | 파드 재시작/OOM | F05-H, F05-R |

F05-H는 `restart_count` 1·2·3이 오를 때마다 checkout 5xx가 **톱니파로 따라 오른다**(주입이 먼저, 증상이 나중).
파드가 죽어 있는 동안 실패한 요청이 그 파드의 스팬을 남길 리 없다.
**도구 출력을 그대로 믿었으면 6건을 거짓 통과로 오판했을 것이다.**

### 남은 불확실성 (정직하게)
BEFORE 표본이 n=3~33으로 작다. 런 간격 5~6분 + 기저 부하 1~2 rps의 구조적 한계다.
강한 근거는 BEFORE가 아니라 **DURING의 표본 크기와 AFTER의 0.000 복귀**, 그리고 F07-H 대조군이다.
F01-R은 배치 첫 런이라 BEFORE 대조가 아예 없다 — 고유 신호(`blocked_db_sessions`)로만 판단했다.

---

## H. 성공 조건이 시나리오를 식별하지 못한다 — 거짓 통과는 아니나 조용히 오염될 자리

**문자 그대로 동일한 성공 조건:**
```
[F01-H, F06-R, F08-G, F15-G]   checkout_5xx_rate >= 0.05                            ← 단독 조건
[F01-P, F17-R]                 checkout_5xx_rate >= 0.05  AND  payment_error_rate >= 10
```

F01-H(외부 PG 429) / F06-R(외부 PG 30초 hang) / F08-G(Oracle 계좌 잠금 + 배포 위장) / F15-G(재고+계좌 동시 잠금)
— **원인이 전혀 다른 넷이 같은 한 줄로 성공한다.** 성공 조건은 "체크아웃이 5% 이상 실패했다"밖에 말하지 않는다.
F01-P/F17-R 쌍은 성공 조건 **두 개가 완전히 일치**한다. 정답지가 다른 두 시나리오가 관측만으로는 같아 보인다.

식별을 `must_rule_out`에만 의존하고 성공 신호에는 판별력이 없다.
**F18-P와 같은 계열이다** — 신호가 주입에 고유하지 않으면 판정이 원인을 증명하지 못한다.
지금 거짓 통과가 안 난 것은 commerce 부하가 5xx를 안 만들어서일 뿐이다(F07-H).
**부하 프로파일이 바뀌거나 배경 5xx가 생기면 이 넷은 동시에 거짓 통과로 돌아선다.**(가설 — commerce에서 재현한 적 없음)

우선순위: **B·A보다 낮고 "무스코프 findPending"보다 높다.** 지금 오염 중은 아니지만, 오염되면 조용히 오염된다.

---

## 임계 재보정의 세 제약 (F03-H 강도 조사에서 도출)

강도가 무작위로 흔들린 게 아니라 **우리가 바꿨고 커밋에 기록돼 있다**는 것이 좋은 소식이다.
다만 §I(F20-R)·§K(F09-R)·§F05-P 재보정에 제약 셋이 생긴다.

**(1) 실측값에는 사다리 파라미터 버전을 함께 기록한다.**
F03-H의 "max 2,904ms"와 "32,720ms"는 **다른 시스템의 측정값**이다.
"언제 측정했나"만으로 부족하고 **"어느 사다리 파라미터에서 측정했나"**가 필요하다.
지금 이 문서의 실측 표에는 그 열이 없다 — 재보정 시 채울 것.

**(2) 사다리 파라미터를 바꾸면 그 시나리오의 과거 실측이 전부 무효가 된다.**
> ⚠ **`32c4308`이 F05-P·F25-H·F03-H 셋의 사다리를 동시에 바꿨다(2026-08-07T07:52:46Z).**
> **그 셋에 대해 08-07 07:52 이전 실측은 재보정 근거로 쓸 수 없다.**
> 특히 **F05-P는 사다리에 6250 단이 새로 끼워졌다** — §F05-P의 게이지 수리 후 재실행 시 이 점을 감안할 것.

**(3) 임계와 강도를 같은 주기에 함께 바꾸지 않는다.**
F03-H가 실증했다 — 강도를 올리니 대조 팔 결함이 드러났다.
같은 배포에서 임계도 낮췄다면 "임계 덕인가 강도 덕인가"를 못 가린다.
**§I·§K는 강도를 고정한 채 임계만 움직이거나 그 반대여야 한다.**

---

## 측정 창을 자를 때 (오늘 두 번 당했다)

**임의로 잡은 BEFORE 창은 거의 항상 직전 런의 주입을 문다.** 배치 런 간격이 5~6분이기 때문이다.
- 내가 KST/UTC를 혼동해 F19-P 주입 전체를 "조용한 구간"에 넣고 셌다 → 결론이 반대로 나왔다
- 감사 에이전트가 F08-G BEFORE를 17:05부터 잡아 0.96을 얻었다 → 직전 F01-P 주입이 17:07:55까지 돌고 있었다. 잘라내니 **0.000**

**규칙 1: BEFORE는 반드시 직전 런의 `t2` 이후로 자른다. 시각대(UTC/KST)를 명시한다.**

> ### ★ 규칙 2는 **세 번째 재발**의 산물이다 (2026-08-07 23:50 발견)
>
> 이 함정은 오늘 처음 밟은 게 아니다:
> ```
> 08-03  F05-H·F08-P 에서 "live.json 조기 소멸"로 오진 → 기록에 남음
> 08-06  F15-R 조사에서 그 오진을 발견하고 정정 (64390fc)
> 08-07  내가 F20-R에서 같은 오진 → 확정 커밋(07001ec) → 반증(fa4fcfe)
> ```
> **`64390fc`(08-06)가 이미 정확히 같은 진단을 커밋 메시지에 적어뒀다:**
> > *"live.json 조기 소멸은 오진이었다 — 'No such file'은 전부 **중단 결정 이후 cleanup이 지운 파일을
> > recovery 틱이 읽은 흔적**이고, success로 끝난 런에도 똑같이 있다."*
> > *"08-03 기록의 F05-H·F08-P '조기 소멸'도 같은 오진이었음을 확인."*
>
> **진단은 있었는데 규칙으로 승격되지 않아 하루 뒤 세 번째로 밟았다.**
> 커밋 메시지에 적힌 지식은 다음 사람을 막지 못한다. **규칙으로 박아야 막는다.**

**규칙 2: `aborted`·`recovery` phase 틱은 판정의 *결과*이지 원인이 아니다.**
중단 원인을 찾을 때는 **`evaluating` phase 틱만** 본다.
- 나는 F20-R의 `aborted` 틱에서 `live.json` 부재를 읽고 그것을 중단 원인으로 확정했다 → 반증됐다(§D-2).
  그 파일은 중단 결정 뒤 cleanup이 지운 것이었다. evaluating 30틱은 전부 정상이었다.

**두 규칙은 같은 오류의 두 얼굴이다 — 경계 뒤의 관측을 경계 앞의 원인으로 읽는 것.**
하나는 시간 경계(직전 런), 하나는 상태 경계(phase). 오늘 각각 두 번씩 나왔다.

---

## K. 스킵 16건 전수 규명 완료

| 시나리오 | 이력 | 계열 | 이번 주기 |
|---|---|---|---|
| F18-P | 0승 5패 | 자기 부하가 자기 감별자를 켬 + 아웃박스 교차 배수 | **가능** (§A·§B) |
| **F25-H** | 0승 5패 | **전환 비원자성** + 주입 무효(192Mi로 안 죽음) | **R1 가능**, R2 실측 선행 (§N) |
| **F03-H** | 0승 3패 | **F19형**(오늘 런) + **예산 소진형**(과거 2런) — 두 계열 | **가능** — #11 + 예산 35m (§O·§Q) |
| **F06-P** | — | **감별자 하한(15) > 설계 부하(5rps)** — 29/29 틱 상시 발화 | **가능** — §F 계열 B |
| **F10-H** | 0승 4패 | **베이스라인 환경 결함형**(courier 풀 포화, 주입 무관) | **불가 → parked**, R1 조사 선행 (§P) |
| **F15-P** | — | **게이지가 과대**로 감별자를 켜 승격 차단 + 강도 부족 | **§M-1 선행 후 재실행** |
| **F09-H** | 1승 3패 | **예산 소진형** — 5단인데 예산 30m, 최악 40m | **가능 — 한 줄**(예산 40m) (§Q) |
| **F09-P** | 7월 3승 → 8월 2패 | **§M-2 케이던스형** (60s 게이지 × 3연속 계약) | **원인 규명 선행** (§M-2 확장) |
| F19-P·F19-S | — | 대조군이 주입 구성요소를 공유 | **가능** (감별자 제거) |
| F20-R | 0승 6패 | 도달 불가 임계 + 부하 수명 소진 | **가능하되 실측 선행** (§I·§J) |
| **F11-R** | 5승 1패 (간헐) | **기존 범주 재발** — `60fc295` | **가능** |
| **F09-R** | 0승 6패 | **인과 사슬 부재형** (신호가 주입 전보다 낮다) | **불가 → parked** |
| **F03-P** | 0승 4패 | **새 계열 — 인과 사슬 마지막 고리 부재** | **불가 → parked** |
| **F05-P** | 0승 2패 | **관측 게이지가 blind** (55.35 vs 실제 93.74) | **가능 — 템플릿 한 줄** |
| **F15-T1** | 1승 3패 | **기존 범주 재발** — `776d993` | **가능** |

### F09-R — **코호트는 맞다. 인과 사슬이 없다.** (F20-R형 → 인과 사슬 부재형으로 재분류, parked 권고)

> **정정.** 앞서 "F20-R형. `pricing_p95` 임계를 실측 기반으로 내리면 가능"으로 적었다. **철회한다.**
> **임계를 내려도 안 된다.**

갈림길의 답: **pricing은 그 노드에 있다.** `host.stress` 대상 `192.168.122.184` = tb-w1이고
`testbed-pricing`·`testbed-order` 모두 tb-w1이다. commerce 코호트 전체가 거기 있다. **코호트 선정은 틀리지 않았다.**

트레이스 3구간(BEFORE는 직전 런 F08-P `t2` 이후), commerce SERVER 스팬 p95:

| 서비스 | BEFORE | **DURING** (노드 CPU 99.87%) | AFTER |
|---|---|---|---|
| **commerce-order** | 102.6 ms | **98.9 ms** | 44.7 ms |
| **commerce-pricing** | 4.9 ms | **4.0 ms** | 2.3 ms |
| commerce-payment | 23.9 | 41.5 | 17.0 |
| commerce-cart | 4.5 | 8.9 | 3.7 |
| commerce-product | 6.4 | 11.5 | 6.1 |

**노드 CPU 99.87% 포화 구간에서 order p95가 98.9ms — 주입 전(102.6ms)보다 오히려 낮다.**
어떤 commerce 서비스도 300ms 근처에 못 간다. 최댓값이 order 98.9ms로 임계의 1/3이다.

**→ F20-R형이 아니다.** F20-R은 "신호가 오르는데 임계가 높다". 여기는 **신호가 주입 전보다 낮다.**
**F03-P와 같은 계열 — 인과 사슬의 마지막 고리가 실재하지 않는다.**
서사는 "노드 CPU noisy neighbor → 같은 노드 서비스 p95가 함께 오른다"인데 마지막 화살표가 일어나지 않는다.

기전 가설: CFS 공정 분배. `stress-ng`가 호스트 CPU를 태워도 컨테이너는 cgroup 몫을 받고,
앱이 CPU 바운드가 아니라 지연이 거의 안 오른다. (미확인 — 반증법: 주입 중 컨테이너 `cpu.stat` throttled 확인)

**부수 발견(가설)**: `order_p95` 게이지는 max 324.52ms를 봤는데 트레이스 실측 p95는 98.9ms다. **3배 이상 괴리.**
`live_probes.py:186-200`이 기록한 APM 게이지 톱니파(한 타임스탬프에 24샘플이 겹쳐 임의 지점을 읽음)와 형태가 같다.
**`order_p95 >= 300`이 간간이 성립한 것 자체가 게이지 잡음일 수 있다.**

수리 선택지가 전부 무겁다: stress 강도 대폭 상향(노드를 죽이면 F05-P와 신호 겹침) /
CPU 바운드 워크로드를 코호트에 추가(**앱 변경**) / success 조건을 CPU throttling으로(**F12-H와 겹침**).
→ **parked 권고.** 배포 전 판단(`cpu_throttled` 베이스라인 미확보)도 유효하나 **그것을 해결해도 이 층이 남는다.**

#### 아래는 정정 전 기록 — `pricing_p95 >= 300`인데 게이지 실측 상한이 14.61

evaluating 80틱:

| 신호 | 임계 | 실측 max | |
|---|---|---|---|
| `node_cpu_util` | ≥ 85 | 99.87 | 충족 |
| `order_p95` | ≥ 300 | 324.52 | **겨우** 충족 |
| **`pricing_p95`** | **≥ 300** | **14.61** | **1/20** |

**노드 CPU 99.8% 포화에서도 pricing은 14.61ms다.** F20-R과 판박이 — 만족 불가능한 조건이 사인을 바꿔가며 위장했다
(초기 3런 `must_rule_out_detected`, 이후 3런 `calibration_levels_exhausted`).

배포 전 판단이었던 "`cpu_throttled` 클린 베이스라인 미확보"는 **다른 층이고 이 층이 더 근본이다.**
베이스라인을 확보해도 `pricing_p95 >= 300`은 안 선다.

⚠ **`order_p95`도 324.52로 임계 300에 겨우 걸친다. pricing만 고치면 이번엔 order가 3틱을 못 채운다. 둘을 함께 보정할 것.**
⚠ pricing이 CPU 포화에 둔감한 이유(경량 서비스인가 / 그 노드에 없는가) 미조사 — 후자면 임계가 아니라 **코호트 선정**이 틀린 것이다.

### F03-P — 인과 사슬의 마지막 고리가 실재하지 않는다 (새 계열, parked 권고)

evaluating 18틱:

| 신호 | 임계 | 실측 | |
|---|---|---|---|
| `payment_p95` | ≥ 500 | **max 1112.45** | 충족 |
| **`checkout_5xx_rate`** | **≥ 0.05** | **18틱 전부 0.0** | **한 번도 안 움직임** |

**payment 풀을 3까지 조여 p95가 1,112ms까지 올랐는데 체크아웃은 단 한 건도 실패하지 않았다.**

> **F20-R형과 다르다.** F20-R은 "신호는 오르는데 임계가 높다". 여기는 **신호가 아예 안 움직인다.**
> 임계를 아무리 낮춰도 0.0은 0.05를 못 넘는다.
> 서사는 "커넥션 대기 → 결제 지연 → **다시 체크아웃 실패로**"인데 **마지막 화살표가 일어나지 않는다** —
> payment 지연 1.1초가 order의 타임아웃 안에 있어 5xx로 승격되지 않는다.

수리 선택지 셋이 전부 무겁다: order 타임아웃을 1초 아래로(앱 변경, 다른 시나리오 전제 건드림) /
풀을 3보다 더 조임(사다리 하한이 이미 2라 여유 없음) / `checkout_5xx_rate`를 빼고 payment 지연만으로 판정
(**→ F19-P·F20-R과 신호가 겹쳐 §H형 결함을 새로 만든다**).
→ **parked 권고.** 배포 전 판단("`db.workload` 스텁, 프로파일 구현 선행")도 맞겠지만
**프로파일을 구현해도 이 층이 남는다. 인과 사슬 검증이 먼저다.**

### F05-P — **게이지가 blind다. 템플릿 한 줄 수리.** (parked 권고 철회)

> **정정.** 앞서 "게이지 → 임계 → 사다리 셋을 고쳐야 하니 이번 주기 불가"로 적었다. **철회한다.**
> 게이지를 고치면 임계는 손댈 필요조차 없을 수 있다.

러너가 쓰는 시리즈(`live_probes.py:178-181`):
```python
"kcm-node-memory-utilization-v1": 'max without(grade) (kcm.node.mem_utilization{node="%s"})'
```

같은 창·같은 노드(F05-P 18:22:44~18:31:54Z, tb-w1, step 30s, n=34):

| 시리즈 | min | **max** |
|---|---|---|
| `mem_utilization` (**러너가 쓰는 것**) | 44.95 | **55.35** |
| `system_mem_utilization` (쌍둥이) | 45.39 | **93.74** |
| `mem_usage / mem_capacity × 100` (직접 계산) | 45.39 | **93.74** |

직접 계산이 `system_mem_utilization`과 **소수점까지 일치**한다. **실제는 93.74%였고 러너는 55.35%를 봤다.**
노드가 NotReady로 죽은 이유가 완전히 설명된다.

#### CPU와 **완전히 같은 병**이다 — 그리고 소스 주석이 아니라고 단언했다

`live_probes.py:162-172` 주석은 CPU 결함(파드 requests를 재서 노드가 타도 안 움직임)을 기록하며 이렇게 적었다:
> *Its memory twin does NOT have this defect (measured the same day: mem_utilization 58.3 vs 57.2 real), so only CPU moves here.*

**이 판단이 틀렸다.** 58.3 vs 57.2는 **평시 측정**이라 우연히 가까웠던 것이다.
`stress-ng`는 파드가 아니라 **호스트에서 직접 도는 프로세스**라 파드 집계 게이지에 안 잡힌다.
**압박이 걸려야 갈리고, 실제로 55.35 vs 93.74로 갈렸다.**

> **일반화할 교훈: 게이지를 평시에 검증하는 것은 검증이 아니다.**
> 게이지가 필요한 순간은 압박이 걸렸을 때이고, 결함은 바로 그때만 드러난다.
> 오늘의 "측정이 거짓말한다" 계열 중 가장 잘 숨어 있던 사례다 — **정확히 반대 결론이 주석으로 못박혀 있었다.**

#### 수리 — 템플릿 한 줄
```python
'max without(grade) (kcm.node.system_mem_utilization{node="%s"})'
```
실측 93.74가 success 임계 `>= 92`를 넘는다. `must_rule_out: < 40`도 안전(실측 min 45.39).
※ 여유가 1.74pp뿐이라 재실행 확인 필요. **반증법: 게이지 교체 후 F05-P 1회 실행.**

#### ⚠ 영향 범위 — **F15-P가 아직 안 돌았다. 이게 시급하다.**

`node_mem_util` 사용 시나리오는 **둘뿐**이다:

| 시나리오 | 노드 | success | must_rule_out | 상태 |
|---|---|---|---|---|
| F05-P | tb-w1 | `>= 92` | `< 40` | 이번 배치 스킵 |
| **F15-P** | tb-w2 | `>= 75` | `< 60` | **미실행 — 큐에 남아 있다** |

(F05-R·F15-T1·F20-Q도 메모리를 보지만 **컨테이너 단위**라 무관.)

**예측 (측정 전에 박아둔다): F15-P는 blind 게이지 때문에 F05-P와 같은 자리에서 실패한다.**
F15-P는 결함이 **둘 겹쳐 있다** — 이 게이지 + `core-banking/surge.js:82` 무제한 조회(§B).
배치 중 러너를 바꾸지 않으므로 이번 배치에서는 그대로 돌린다. 빗나가면 게이지 진단을 다시 봐야 한다.

> ### ★ 결과 (2026-08-07 22:59) — **예측 반증. 그리고 게이지의 성격이 바뀌었다.**
>
> `F15-P-run-1b574543` (t1 22:46:11Z) → `aborted`. **"실패"는 맞지만 이유가 예측과 다르다.**
> 사전 등록 B-1의 **제3의 결말**(등록에 미리 넣어둔 것)에 해당했다:
> `max(A) 60.39 < 75` **AND** `max(B) 64.25 < 75` → **게이지 문제가 아니라 주입 강도 부족.**
>
> #### ★ blind 게이지의 방향이 **노드마다 반대다** (확정)
>
> | | 노드 | A(러너가 쓰는 것) | B(참값) | |
> |---|---|---|---|---|
> | **F05-P** | tb-w1 | max **55.35** | max **93.74** | **과소** |
> | **F15-P** | tb-w2 | max **60.39** | L0 max **48.51** | **과대** |
>
> **`mem_utilization`은 "낮게 보이는 게이지"가 아니라 실제와 무관한 값이다.**
> 위 §F05-P에 "blind라 압박을 못 본다"고 쓴 규정을 **"실제와 상관없는 값을 낸다"로 정정한다.**
> **보정으로는 해결 불가이고 교체가 유일한 답이라는 근거가 오히려 강해졌다.**
> (원인 가설: A가 파드 memory **requests** 집계 — Oracle이 큰 request를 잡은 tb-w2에서는 과대,
> commerce가 실사용으로 request를 넘는 tb-w1에서는 과소. **미검증.**)
>
> #### 두 층이 겹쳐 있다
> - **중단의 직접 원인은 게이지다** — 참값 60.72인데 러너가 57.32를 보고 `node_mem_util < 60`(`cpu-only-pressure`)을 켰다.
>   **참값이었다면 이 감별자는 발화하지 않았다.**
> - **근본은 강도 부족이다** — 참값 max 64.25가 success 임계 75에 미달. 게이지를 고쳐도 success는 안 섰다.
>
> #### 단서 — L2 미도달이 강도 부족의 원인일 수 있다 (가설)
> 참값이 단조 증가 중이었고(L0 p50 48.08 → L1 60.72) 사다리 vm 총량이 1500M→3000M→**5000M**이라
> **L2는 L1의 1.67배**다. **L2까지 갔으면 75를 넘었을 수 있는데, L2에 못 간 이유가 게이지가 켠 감별자다.**
> **§P(F10-H)와 같은 형태 — 감별자가 승격을 가로챘다.**
> 반증법: **§M-1 수리 후 F15-P 1회 재실행.** L2 도달 + 참값 75 초과면 확인, 미달이면 진짜 강도 부족.
>
> #### **§M-1 시급성은 줄지 않고 늘었다**
> F05-P는 과소로 success를 막았고 F15-P는 과대로 승격을 막았다.
> **방향이 반대인데 둘 다 판정을 망쳤다.** → **F15-P 재실행 전에 반드시 넣는다.**

### ~~F05-P — 관측 게이지가 틀렸다 (parked 권고)~~ (아래는 정정 전 기록)

| elapsed | `node_mem_util` | `node_ready` |
|---|---|---|
| 63~95s | 46.85 | True |
| **121s** | **46.85** | **False** ← abort |

success 조건은 `node_mem_util >= 92`인데 **28틱 내내 최댓값이 55.35%다. 그 상태에서 노드가 NotReady로 죽었다.**
메모리 55%인 노드는 죽지 않는다 — **게이지가 실제 메모리 압박을 보지 못한다.**

내가 blind-metric 판독으로 중간 사다리 단을 잘못 승인한 근본 원인이 이것이다.
게이지가 낮게 보이니 더 올려도 안전해 보였는데 실제로는 이미 노드를 죽이는 압박이었다.

**임계만 낮추면 "메모리가 안 올랐는데 성공"이 되어 더 나빠진다.**
`node_mem_util`의 계산식 확인 선행(`MemTotal - MemFree` 계열이면 page cache를 여유로 안 쳐 낮게 나온다 — `MemAvailable` 기반이어야 한다).
게이지 → 임계 → 사다리 순서가 셋이라 이번 주기에 안 맞는다. → **parked 권고.**

### F11-R·F15-T1 — 이미 문서화·테스트 고정된 범주의 재발 (독립 수렴)

**F11-R → `60fc295`** ("감별자가 성공의 부수 효과를 실격으로 읽는 다섯 번째 범주", §23에 `business_ok` 건으로 등재).
오늘 실측: 발화 두 틱에서 `checkout_5xx_rate` 0.964·`business_ok` False로 **success 조건이 이미 서 있었고**,
`achieved_rps`가 62 → 6.97 → 0.81로 떨어져 `user-load-not-flowing`(`lt 30`)이 발화했다.
급락 이유는 로드젠 사망이 아니라 **체크아웃이 99% 실패해 k6 여정이 진행되지 못한 것** — 즉 성공의 부수 효과다.
수리: `achieved_rps` 대신 **로드젠 프로세스 생존**을 보게 한다(`loadgen_monitor.py`가 `tagged_pids`로 이미 추적).
원래 재려던 것이 그것이다. success 조건 성립 시 면제하는 안은 평가 순서 의존을 만들어 권하지 않는다.

**F15-T1 → `776d993`** (테스트 `test_success_and_abort_do_not_share_a_signal_in_the_same_direction`,
F05-H·F05-R·F15-T1·F25-H 4건 등재).
`776d993`이 더 날카로운 기전을 이미 담고 있다: **abort는 min_hold 앞(`adaptive.py:229`), success는 뒤(:264)**.
같은 신호가 둘을 같은 방향으로 굴리면 성공 가능 구간은 두 임계 사이 창뿐인데,
**그 창이 min_hold 안에서 닫히면 성공은 평가조차 못 되고 abort만 발화한다.**
오늘 런: 47s에 이미 restart=2, 94s에 3 도달. success streak 3과 abort streak 2가 동시에 찼다.
수리: **abort 예산 상향(3→5)과 대기 중인 `min_hold 240s→60s`를 함께.**
min_hold 단축만으로는 부족하다 — 오늘 런은 47s부터 restart=2였다.

---

## L. 레벨 전환의 cleanup → apply 공백 창 (확정, D-2 철회의 후속)

`adaptive_runtime.py`에서 `_cleanup_for_level_transition()`(`:427`)이 `_apply_current_level()`(`:545`)보다 **먼저** 돈다:

```
1) escalate 성립 → level_index += 1
2) _cleanup_for_level_transition()   ← 이전 부하 종료 (live.json 소실)
3) _apply_current_level()            ← 새 부하 적용 (live.json 재생성)
```

**2와 3 사이에 부하 공백 창이 있다.** 그 창에 판정 틱이 겹치면 관측 불가가 된다.
`level_changes`는 apply 성공 시에만 증가하므로(`:617` 주석), `level_index=1` + `level_changes=1`은
"2는 됐고 3 전에 틱이 돌았다"는 서명이다.

**"승격 apply가 실패했다"는 가설은 반증됐다** — apply 실패 경로는 reason을 `profile_apply_failed:{...}`로 쓰는데(`:578-587`)
F03-P·F20-R의 reason은 `decision_observation_unavailable`이다.

**D-2 철회와 모순되지 않는다.** 승격 자체는 무해하고, 문제는 **전환 구현의 순서**다.
창을 밟느냐가 타이밍이라 **간헐적**이다 — 같은 F03-P라도 `5edd6309`는 승격 2회를 무사히 넘겼고 `27f6eee2`만 밟았다.
§J(부하 수명 소진)와 형제지만 별개다 — J는 수명이 다한 것이고 이건 의도적으로 껐다 켜는 사이다.

⚠ 공백 창의 실제 길이는 미측정. 반증법: 전환 전후 타임스탬프로 공백 길이와 틱 주기(15s) 비교.

---

## M. 게이지 전수 감사 — blind는 둘이 전부. p95는 표본 부족.

### M-0. 판별 규칙 — 기전을 몰라도 이것으로 충분하다 (확정, 2026-08-07 23:10)

> **`kcm.node.*_utilization` 중 `system_` 접두가 없는 것은 신뢰할 수 없다.**
> **`system_` 버전은 `usage / capacity`와 소수점까지 일치하며 참값이다.**

tb-w2 @22:52:00Z 순간값이 이를 확정한다:
```
system_cpu_utilization = 83.094  =  cpu_usage/cpu_capacity = 83.094   ← 일치
cpu_utilization (비-system)      = 26.615                              ← 참값과 불일치
cpu_request/cpu_capacity         = 47.500                              ← 이것과도 불일치
```

#### "파드 requests 집계" 가설은 **반증됐다**

| | tb-w1 (F05-P 창) | tb-w2 (F15-P 창) |
|---|---|---|
| A `mem_utilization` | 46.18 ~ **55.35** (변동) | 56.89 ~ **60.39** (변동) |
| `request/capacity` | **56.52 (상수)** | **42.57 (상수)** |
| `usage/capacity` (참값) | 51.80 ~ **93.74** | 38.00 ~ **64.25** |

**requests는 파드 스펙이라 런 중 안 바뀌는데 A는 변한다. 값도 안 맞는다.**
→ **A는 requests가 아니다.** `limit/capacity`도 아니다.

> ⚠ **`live_probes.py:170-172` 주석의 설명이 부정확하다** — "cpu_utilization은 파드 CPU requests를 추적한다"고
> 적혀 있으나 requests/capacity는 47.5인데 게이지는 26.6이다. **결론(blind다)은 맞고 설명이 틀렸다.**
> 게이지 교체 시 이 주석도 함께 정정할 것.

**A의 정확한 정의는 미확정이다**(역산 분모 7.89GB가 `capacity−usage` 7.76GB와 1.7% 차이나 결론 안 냄).
**그래도 실무에 지장이 없다** — 위 판별 규칙이 기전과 무관하게 성립하기 때문이다.

#### 범위 확정 — **§M-1 수리는 여전히 한 줄이다**
이 규칙에 해당하는 게이지는 CPU·메모리 **둘뿐**이고(`.system_` 전수 조회), **CPU는 이미 수리됐다.**
- `kcm.pod.*_utilization_by_limit/_by_request/_by_node_capacity` — **이름이 분모를 명시**하므로 모호성이 없고 **현재 미사용**
- 나머지 템플릿 9개는 절대 카운터·앱 에이전트 계측이라 "노드를 파생 지표로 재는" 구조가 아니다

### M-1. blind 쌍둥이는 **정확히 둘**이고 더 없다 (확정)

VictoriaMetrics `__name__` 전수에서 `.system_` 접두 시리즈:
```
kcm.node.system_cpu_utilization    ← 이미 수리됨
kcm.node.system_mem_utilization    ← 오늘 발견, 미수리 (§F05-P)
```

**"못 찾겠다"가 아니라 "찾아봤고 둘뿐이다".** 나머지 템플릿 9개는 이 구도가 성립하지 않는다 —
**blind 결함의 조건은 "노드 전체 vs 파드 집계"라는 두 관점이 존재하는 지표**인데
그건 노드 레벨 CPU·메모리뿐이고, 파드 레벨은 절대 카운터라 그 모호성이 없다.

> **잠재 함정(현재 무해)**: `kcm.pod.*`에 `_by_limit` / `_by_request` / `_by_node_capacity` 세 변형이 있다.
> **현재 템플릿은 이 계열을 하나도 쓰지 않는다.** 누가 나중에 파드 사용률을 배선하면 같은 병이 재발할 수 있다.

### ★ M-2 규정 확장 (2026-08-08 00:10, F09-P가 반증) — **표본 부족은 하위 형태 하나일 뿐이다**

§M-2를 "표본 부족"으로 규정했는데 **F09-P에서 반증됐다.** 표본이 레벨당 **6,720~8,046**으로 충분한데 같은 증상이 난다.

**규정을 넓힌다: 「APM p95 게이지가 '연속 N틱' 계약과 맞지 않는다」.** 하위 형태 둘:

| | **표본 부족형** (F09-R) | **케이던스·비지속형** (F09-P · F10-H) |
|---|---|---|
| 표본 | 분당 4~42 | **분당 343~993 (충분)** |
| 게이지 요동 | 추정 잡음 | **실재 현상** (트레이스 분당 p95도 40.6~4,604로 함께 요동) |
| 게이지 vs 트레이스 | 2~3배 어긋남 | 대체로 수렴 |
| 3연속 불성립 이유 | 값이 임의로 튐 | **60초 케이던스 × 스파이크가 180초를 못 감** |
| 수리 | 창 확대 / 표본 하한 | **신호 교체(케이던스 짧은 것)** |

**같은 뿌리다** — "연속 N틱 계약이 60초 케이던스 게이지에서 성립하기 어렵다". 수리 축도 같아 계열을 나누지 않는다.

#### F09-P 실증 — success 두 조건이 동시 성립한 틱이 8개인데 **streak이 1에서 안 오른다**
```
el=204  throttled=40,862  gateway_p95=6,766.1   streak.success=1
el=220  throttled=40,862  gateway_p95=7,216.9   streak.success=1   ← 같은 값 반복
el=235  throttled=40,862  gateway_p95=7,216.9   streak.success=1
el=251  gateway_p95=U                                              ← 끊김
```
`prometheus.apm_service_p95`는 **`update_interval` 60초**이고 틱은 15초다 → **4틱마다 한 번만 새 표본**.
러너가 중복 표본을 안 세므로 **3연속 = 독립 표본 3개 = 180초 지속**을 요구하게 된다. (가설, 강함 — 코드 미확인)

> **F10-H가 `f60c903`으로 이미 고친 것과 정확히 같다** — 성공 신호를 `order_p95` → `order_create_5xx_rate`(30초 케이던스)로 교체.
> **F09-P는 그 수리를 못 받았다.**
> ⚠ 다만 F10-H는 그 교체가 "대조 팔 없는 절대 임계"를 들여 §P로 이어졌다. **신호 선택에 신중해야 한다.**

#### F09-P 이력 — **7월 3승 → 8월 2패로 전환됐다**
```
07-20 · 07-21 · 07-26   succeeded, 레벨 0에서 183·183·179초 (min_hold 120초 직후 3연속 성립)
08-07 08:30 · 23:39     failed, 3단 다 쓰고 1599·1605초
```
**레벨 0은 7월과 동일하다.** 2단째만 `constrained-100m` → `225m`으로 바뀌었다.
**무엇이 바뀌어 250m 조임이 더 이상 gateway p95를 500 이상으로 지속시키지 못하는지 미규명.**
후보: commerce orders 1,185,713행 증가 / 부하 패턴 / 앱 변경. **조사 안 했다.**

> **수리 판단(내 결정)**: **임계 조정 전에 7월→8월 전환의 원인 규명이 선행돼야 한다.**
> §I 제약대로 강도를 고정한 채 임계만 움직여야 하는데, **무엇이 바뀌었는지 모르는 상태에서 임계를 맞추면 다음에 또 어긋난다.**
> R1(신호 교체)도 F10-H의 전례상 새 결함을 들일 수 있으므로 규명 후 판단한다.

#### 다른 계열은 해당 없음 (확정)
`§Q 예산` 1605초 < 1800초(여유율 125%) / `§L 전환` `level_changes=3` 정상 기록 /
`§F 계열 A` `achieved_rps > 65`는 미발화 — **다만 실측이 60.0~71.0으로 임계를 여러 번 넘었다(여유 5). 잠재 위험으로 기록.**

---

## R. 미규명 — **"레벨을 올렸는데 피해가 줄어든다"가 세 시나리오에서 반복된다**

| 시나리오 | 주입 강화 방향 | 피해 |
|---|---|---|
| **F10-P** | disk median 50 → 60.5 | account p95 1,877 → 672 ms |
| **F15-P** | vm 1500M → 3000M | (참값 메모리는 올랐으나 승격 전 중단) |
| **F09-P** | CPU 250m → 225m → **200m** | gateway p95 370.8 → 321.4 → **72.2 ms** |

### ★ 조사 결과 (2026-08-08 00:30) — **두 가설 다 반증. 셋은 같은 기전이 아니다.**

#### 처리량 급감 가설 — **반증**
F09-P `commerce-gateway` 레벨별:

| 레벨 | n | **rps** | **p95** | **err_rate** |
|---|---|---|---|---|
| L0 (250m) | 7,930 | **14.31** | **370.8 ms** | 0.0121 |
| L1 (225m) | 8,046 | **14.52** | **321.4 ms** | 0.0547 |
| L2 (200m) | 6,720 | **13.71** | **72.2 ms** | **0.0006** |

**처리량은 4% 감소일 뿐인데 지연은 5배 낮아졌다.** "부하가 빠져서 빨라진 것"이 아니다.

#### 거절 가설 — **반증**
요청이 거절돼 줄어든 거라면 5xx/4xx가 올라야 하는데 **정반대다.**
F09-P 에러 건수 440 → **4**, F10-P account 25 → 10 / transfer 58 → 31. **앞단에서 막힌 것이 아니다.**

#### 셋은 다른 현상이다

**F09-P — L2에서 주입 자체가 실효를 잃은 것으로 보인다 (가설, 유력)**

| | L0 (250m) | L1 (225m) | **L2 (200m)** |
|---|---|---|---|
| inventory p95 (주입 대상) | 600.2 | 903.0 | **23.0 ms** |
| order p95 | 7,733.1 | 11,551.9 | **197.6 ms** |
| gateway p95 | 370.8 | 321.4 | **72.2 ms** |
| inventory rps | 3.40 | 2.88 | **3.81 (증가)** |

> **결정적 모순: `inventory_throttled`가 L2에서 더 낮다** (L0 12,260~67,780 대 L2 10,157~33,832).
> **CPU limit을 더 조였는데 스로틀이 덜 걸린다 — 부하가 같다면 물리적으로 반대여야 한다.**
> → **L2에 실제 limit 200m가 적용되지 않았을 가능성이 유력하다.** 직접 확인은 못 했다
> (런 종료로 baseline 복구, 트레이스의 `k8s_pod_name`이 비어 파드 교체 추적 실패).

**F10-P — 선택적 회복. 전면 정상화가 아니다.**
주입은 계속 작동 중이다(transfer 16.6초 → 15.1초로 여전히 아프다). **account만 1,877 → 672로 떨어졌다.**
transfer는 rps −27%인데 p95 −9%, account는 rps −7%인데 p95 −64% — **두 서비스가 정반대 방향이라
처리량으로 설명되지 않는다.** 이유 **미규명.**

**F15-P — L2 미도달로 데이터 없음.**

### ★ §I 제약에 한 줄 추가 — **주입 유효성의 독립 증거를 병기한다**

> **임계 실측값에는 "그 구간에 주입이 실제로 걸렸다는 독립 증거"를 병기한다**
> (주입 대상 자체의 신호, 또는 주입 실행기의 상태값).

**F09-P L2가 그 필요를 보여준다.** 주입이 안 걸린 구간의 실측을 임계 근거로 쓰면
**주입 없는 상태를 기준으로 임계를 정하게 된다 — 가장 위험한 형태다.**
그리고 F10-P처럼 **서비스별로 레벨에 따라 반대로 움직이는 경우**도 있으므로,
어느 서비스의 신호를 기준으로 삼는지도 함께 적어야 한다.

### ★★ 규명 (2026-08-08 00:35) — **주입이 절반만 걸렸다. 파드가 공존했다.**

`kcm.pod.cpu_limit{pod=~"testbed-inventory.*"}` 레벨별 실측(파드 단위, 밀리코어):
```
BEFORE   6db9bd667d-qsctv  500m  n=11        ← baseline 단독
L0 250m  6d65f47bcd-t2wxc  250m  n=18        + baseline 500m n=5·2   ← 전환기 잔존(짧음)
L1 225m  77f6966686-w9g4t  225m  n=18        + baseline 500m n=3·4   ← 〃
L2 200m  7b97b7bf68-7kl8r  200m  n=17        + 6db9bd667d-m4fds 500m n=17  ← ★ 구간 전체 공존
```

**L0·L1에서는 baseline 파드가 n=2~5(60~150초, 롤아웃 전환기)로 짧게만 있었다.
L2에서만 n=17 ≈ 510초로 레벨 구간 전체를 덮는다.**

#### 이것이 네 관측을 전부 설명한다

| 관측 | 설명 |
|---|---|
| gateway p95 370.8 → 321.4 → **72.2** | 트래픽 절반이 **제한 없는 500m 파드**로 갔다 |
| inventory p95 600 → 903 → **23** | 〃 |
| order p95 7,733 → 11,551 → **197** | 〃 |
| **`inventory_throttled`가 L2에서 더 낮다** | 절반이 스로틀 없는 파드로 갔다 — **모순 해소** |
| inventory rps 3.40 → **3.81 증가** | 파드가 둘이라 처리 용량이 늘었다 |
| err_rate 0.0547 → **0.0006** | 여유 있는 파드가 처리했다 |

> **"레벨을 올렸는데 피해가 줄었다"가 아니라 "주입이 절반만 걸렸다"이다.**

#### §L의 **세 번째 형태**이고 **가장 위험하다**

| 형태 | 사례 | 증상 |
|---|---|---|
| cleanup→apply 공백에 판정 틱이 겹침 | F20-R·F03-P | 런이 **죽는다** |
| 종료 검증이 즉시 단정 | F25-H | 런이 **죽는다** |
| **전환 후 이전 파드가 정리되지 않고 공존** | **F09-P** | **런이 조용히 계속 돌며 오염된 실측을 남긴다** |

**앞의 둘은 눈에 띄지만 이건 안 띈다.**
그리고 **`level_changes`가 3개 정상 기록됐다** — 전환 서명(`level_index` 증가 + 미기록)이 없는데도 전환이 불완전했다.
**기존 서명으로는 이 형태를 못 잡는다.**

#### ⚠⚠ 임계 재보정 전체에 대한 함의

**k8s 롤아웃 계열(`k8s.env`·`k8s.patch`·`k8s.resource`) 주입에서 승격이 일어난 레벨의 실측은
파드 공존 여부를 확인하기 전까지 신뢰할 수 없다.**
§I(F20-R)·§Q·§K의 재보정이 전부 이 확인을 통과해야 한다.
→ **방금 추가한 §I 제약("주입 유효성의 독립 증거")의 구체적 검사법이 바로 이것이다:**
**`kcm.pod.cpu_limit`(또는 해당 자원) 시리즈를 파드 단위로 레벨 구간에 걸쳐 확인한다.**

#### F09-P 수리안 갱신 — 우선순위가 바뀐다
| # | 항목 |
|---|---|
| **R0 (신규·선행)** | **L2 실측을 임계 근거에서 제외.** 그 구간은 "주입 절반" 상태다 |
| **R1** | **전환 시 이전 ReplicaSet의 파드가 0인지 확인.** §14(R1) 폴링 수리와 같은 계열 |
| R2 | 성공 신호 케이던스(§M-2) — **R1 이후 재측정해야 의미가 있다** |

앞서 "신호 교체 전에 7월→8월 원인 규명"이라 했는데 **이제 R1이 먼저다.**
L2가 오염된 상태에서 임계·신호를 조정하면 잘못된 기준이 박힌다.

#### 7월→8월 전환에 대한 함의 (가설)
**7월 3승은 전부 레벨 0에서 183초 만에 성공했다 — 승격을 한 번도 안 했다.** 8월 두 런은 3단을 다 밟았다.
→ **승격할수록 파드 공존 오염에 노출된다.** ※ 7월 런의 파드 공존 여부는 미확인.

#### 다른 두 시나리오는 이 기전이 아니다 (확정)
F10-P·F15-P는 `host.stress`(fio·stress-ng)라 **파드 롤아웃을 일으키지 않는다.**
**"셋은 같은 기전이 아니다"가 유지되고 그중 F09-P만 규명됐다.**
**F10-P의 account 선택적 회복은 여전히 미규명.**

※ 두 파드 간 실제 트래픽 분배 비율은 미측정(트레이스의 `k8s_pod_name`이 비어 있다).

### ★ 오염 범위 전수 (2026-08-08 00:40) — **§G 결론 유지. 구조적 오염 가설은 반증.**

k8s 롤아웃 계열 + 승격 1회 이상인 런은 **4건**이고 **그중 통과는 F12-H 하나**다:
```
F12-H-run-275cc626  succeeded  k8s.patch  3단 승격2   ← 유일한 통과
F03-P-run-5edd6309  failed     k8s.env    3단 승격2
F09-H-run-9d46c50f  aborted    k8s.env    5단 승격4
F09-P-run-ed63c549  failed     k8s.patch  3단 승격2   ← 규명 완료
```

#### F12-H는 깨끗하다 — success 구간이 주입 파드 단독이었다 (확정)
```
15:40:15 ~ 15:42:00   jld78=175m  tnppp=500m     ← 공존 (약 105초)
15:42:15 ~ 15:44:45   jld78=175m                  ← ★ 주입 파드 단독
                      ↑
15:42:26  success streak 1 시작 — 공존 종료 직후
```
**success 성립 구간 전체가 주입 파드 단독이다.** → **§G "통과 거짓 0" 결론 유지.**

#### ★ "최상위 단 구조적 오염" 가설 — **반증**
내가 "마지막 레벨이라 다음 전환이 없어 정리 트리거가 없나"를 우려했는데:

| | 최상위 단 공존 지속 |
|---|---|
| **F12-H L2** | **약 105초** — 정상 롤아웃 전환기 수준 |
| **F09-P L2** | **약 510초** — 구간 전체 |

**F12-H도 마지막 레벨인데 105초 만에 정리됐다.** 최상위 단이라서 안 되는 구조가 아니다.
→ **F09-P의 510초는 예외적 사건이다. 파급은 우려한 것보다 훨씬 작다.**
※ **왜 F09-P에서만 510초였는지는 여전히 미규명.**

#### ★ 부수 소득 — "공존이 지연을 낮춘다"가 **확정으로 격상**
F12-H가 같은 런 안에서 자연 실험을 제공했다:
```
공존 구간 (15:40:15~15:42:00)   product_p95      7.2 ~     38.2 ms
단독 구간 (15:42:15~)           product_p95  1,301   ~ 15,902   ms
```
**공존이 끝나는 순간 지연이 수백 배로 폭증했다.** F09-P의 추론이 다른 시나리오·다른 서비스에서 재현됐다.

#### 한계 — env 계열은 이 방법으로 판별 불가
F03-P·F09-H는 `k8s.env`(Hikari 풀 / JVM 힙)라 **CPU limit이 전부 500m으로 동일**해
파드 다중 존재는 보여도 **어느 것이 주입 파드인지 구분할 수 없다**(주 파드 n=14~15 + 잔존 n=1~3, 정상 롤아웃과 구별 불가).
**둘 다 통과가 아니라 §G에는 영향 없다.** 나중에 env 계열 통과 런이 나오면
다른 지표(파드별 JVM 힙 한계, DB 커넥션 수 등)가 필요하다.

#### §14(R1) 수리안 갱신 — **하드 에러 대신 기록으로**
전환 후 정리는 **보통 잘 된다**(F12-H 105초). **드물게 길어지는 게 문제**다.
→ **판정 시작(min_hold 진입) 전에 이전 파드가 남아 있으면 그 사실을 런 산출물에 기록한다.**
그러면 사후에 "이 구간 실측은 오염됐다"를 판별할 수 있다.
**넷째 제약("주입이 실제로 걸렸다는 독립 증거")의 기계적 구현이고, 하드 에러보다 과잉 차단이 적다.** — 채택.

---

### M-2. APM p95 — 톱니파는 이미 수리돼 있다. 원인은 **표본 부족**. (아래는 F09-R 기준 원 기록)

**톱니파 가설은 반증됐다.** 템플릿에 `max_over_time([60s])` 붕괴와 `span_count > 0` 가드가 **둘 다 이미 있다.**

그런데 **괴리는 실재하고, 두 대안 설명이 모두 배제됐다:**

| 대안 | 검증 | 결과 |
|---|---|---|
| 통계량 정의 차이 | 트레이스도 **분당 p95의 최댓값**으로 맞춤 | order 145.1 vs 게이지 324.52 (**2.2×**), pricing 4.5 vs 14.61 (**3.2×**) — **잔존** |
| 트레이스 샘플링 누락 | 같은 창 표본 수 대조 | APM `span_count` order 558 / ClickHouse 1,214 — **ClickHouse가 오히려 많다** |

**진짜 원인(가설, 강함): `span_count`가 분당 4~42개다.** 그 표본으로 p95를 내면 사실상 상위 1~2개를 뽑는 것이라 추정이 요동한다:

```
commerce-order percentile95 게이지, 평시 5분:  min 3.17  max 348.63   ← 100배 요동
같은 구간 트레이스 분당 p95:                    66.7 ~ 145.1          ← 안정 (분당 18~82)
```

**F20-R의 `payment_p95` 20/30 unusable과 같은 뿌리다** — 표본 부족.
거기서는 표본 0이라 unusable로 **드러났고**, 여기서는 몇 개라 **잡음이 큰 값으로 통과한다. 후자가 더 위험하다.**
`span_count > 0` 가드는 **0만** 막는다. 4개도 통과시킨다.

### 영향 범위 — **19종**, 위험군 8종 (확정)

| 임계대 | 시나리오 |
|---|---|
| **300 ms** | F09-R(order·pricing), F12-H(pricing, MRO) |
| **400~500 ms** | F02-H(payment), F03-P(payment), F07-H(user), F09-P(gateway), F12-H(product), F20-R(payment) |
| 1000~2000 ms | F02-H(order), F20-R(order), F03-H(order), F15-H·F15-T2(order) |
| 3000~4000 ms | F20-P(transfer), F15-P(transfer·account) |
| recovery 전용 | F09-H, F10-H, F10-P, F20-Q, F02-P |

**임계가 낮을수록 위험하다.** 게이지 잡음 폭(order는 평시에도 3~348ms)이 임계와 같은 자릿수면 판정이 잡음으로 갈린다.
→ **300~500ms 대 8종이 위험군.**

### ⚠ 수리 전에 정해야 할 것 — **어느 계측이 참값인가**
게이지와 트레이스는 **서로 다른 스팬 집합**을 본다. 어느 쪽이 참인지 미확정이다.
**임계 재보정은 이 결정 뒤에만 의미가 있다.**

### 수리 방향 (구현 안 함)
- (a) `span_count` 가드에 **하한 N** 부여 — 단, **N을 올리면 unusable이 늘어 §J형 중단이 늘어난다.**
  가드만 조이면 충돌하므로 "unusable이 판정을 막지 않는" 처리와 **함께** 넣어야 한다
- (b) **창을 `[60s]` → `[180s]`로 확대** — 표본이 3배가 되어 잡음이 준다. 부작용은 신호 지연.
  **임계도 가드도 안 건드려서 더 싸다**(에이전트 판단, 실측 근거 없음)

### M-3. F09-R 판정 재검증 — 게이지로도 확증 (확정)

앞 판정이 트레이스에만 근거했으므로 게이지가 참이면 뒤집힐 수 있었다. **뒤집히지 않았다.**

`commerce-order percentile95` 게이지 3구간:

| 구간 | n | max | mean |
|---|---|---|---|
| BEFORE | 6 | **348.63** | 123.80 |
| DURING (노드 CPU 99.87%) | 23 | **324.52** | 121.27 |

**BEFORE max가 DURING보다 높고 mean은 사실상 같다.** 두 계측이 같은 결론을 준다.

> 에이전트가 자기 측정 실수를 신고했다: AFTER 구간 max 5617.24는 **창이 다음 런 시작을 물어** 나온 값이다.
> 창 규칙 위반이므로 **그 숫자는 근거로 쓰지 않는다.** BEFORE/DURING 비교에는 영향 없어 결론은 유지된다.

### 우선순위

| # | 결함 | 영향 | 크기 | 권고 |
|---|---|---|---|---|
| **1** | 노드 메모리 게이지 blind | **2** (F05-P, **F15-P 미실행**) | 템플릿 한 줄 | **이번 주기 필수** |
| **2** | APM p95 표본 부족 | **19** (위험군 8) | 설계 필요 | 이번 주기 **완화만**, 임계 재보정은 다음 |
| 3 | `kcm.pod.*_by_*` 세 변형 | 0 (미사용) | — | 기록만 |

---

## N. F25-H — 일곱째 계열: **레벨 전환이 원자적이지 않다**

`F25-H-run-4771bc5b` (2026-08-07 20:40:53Z~20:46:03Z UTC), `dirty`, `result.json` 부재.
```
reason:  cleanup_failed after level_transition_cleanup_failed
cleanup: {"succeeded": false, "reason": "load.north_south:recovery failed"}
```

> ⚠ 이 런 디렉터리의 파일 mtime은 **KST**로 표시된다(`08-08T05:46` = UTC `20:46`). 런 내부 타임스탬프는 UTC.

### `776d993` 형태가 **아니다** (확정)

evaluating **16틱 전수**: `pg_restart_count` 0, `pg_termination_reason` `"None"`, `checkout_5xx_rate` 0.0,
`pod_ready` True, `target_health` 200. **`streaks: {}` — success도 abort도 한 틱도 못 찼다.**

`pg_restart_count`가 success(`>=1`)·abort(`>=6`)·escalate(`<1`) 세 곳에 쓰이는 **위험 구조는 실재**하지만
(등재는 유효), restart가 0에서 안 움직여 **이번 스킵의 원인은 아니다.**
abort 예산이 6으로 success(1)보다 훨씬 높아 여유가 큰 점도 F15-T1(1 대 3)과 다르다.

### 층 1 — 주입이 아무 효과도 못 냈다 (확정)
`squeeze-192mi`는 `testbed-postgres` memory limit을 512Mi → 192Mi로 조인다.
**16틱(약 4.5분) 내내 restart 0, OOMKill 0, checkout 5xx 0.0.**
**192Mi로는 postgres가 죽지 않는다.** → escalate(`restart_count < 1`, 3틱) 성립 → 승격 시도 → 거기서 dirty.
**사다리 첫 단이 너무 약하다** — F20-R의 층 2(rps 1)와 같은 형태.

### 층 2 — dirty의 진짜 원인: 종료 검증이 너무 이르다

`mutations.json`:
```
20:45:39  transition-cleanup  k8s.resource       → 20:45:44 완료
20:45:44  transition-cleanup  load.north_south   → 20:45:52 완료
          ↑ 이 직후 recovery 검증 실패 → dirty
```

`load_north_south_executor.py`의 recovery:
```bash
recovery)  check_read_only;  [[ -z "$(tagged_pids)" ]]  ;;   # 태그된 k6가 하나도 없어야 한다
```
**k6에 종료를 요청한 직후 곧바로 "프로세스가 전부 사라졌는가"를 검증한다.** k6는 종료에 수 초가 걸린다.

### 일곱째 계열 — 전환 계약 결함형

> 레벨 승격 시 `_cleanup_for_level_transition()` → `_apply_current_level()` 시퀀스가 **원자적이지 않아,
> 끄는 도중·끈 직후에 판정이나 검증이 끼어들면 런이 죽는다.**

**오늘의 미규명 조각 셋을 함께 덮는다:**

| 사례 | 무엇이 끼어들었나 | 결과 |
|---|---|---|
| **F25-H** | companion 종료 검증이 k6 종료 지연을 안 기다림 | `dirty` |
| **F20-R-0c92dfd2** | cleanup→apply 공백에 판정 틱이 겹침 | `decision_observation_unavailable` |
| **F03-P-27f6eee2** | 동일 (91초 공백) | 〃 |

셋 다 **`level_index`는 올랐는데 `level_changes`에 새 항목이 없다**는 같은 서명.
**§J(부하 수명 소진)와 다르다** — J는 부하가 수명이 다해 자연 종료한 것이고, 이건 **의도적으로 껐다 켜는 사이**다.
증상(관측 부재)이 겹쳐 사인만으로는 구분되지 않는다.

### 이력 — 5런 0승, F20-R과 같은 위장 패턴
`safety_observation_unavailable` ×3 → `evaluation_level_timeout` ×1 → `dirty` ×2.
**"주입이 효과가 없다"는 하나의 사실이 사인을 세 번 바꿔가며 나타났다.**

### 재고 수리는 효과가 있었다 — 그러나 `_note` 가설은 거짓 (확정된 반증)

수리 전(`af660719`) 대 오늘(`4771bc5b`):

| 신호 | 수리 전 | **오늘** |
|---|---|---|
| `entry_status` 409 비율 | 12/14 = **86%** | 5/16 = **31%** |
| `checkout_5xx_rate_baseline` | 0.1176 ×1, 0.0 ×13 | **전 16틱 0.0** |
| 종료 사유 | `cleanup_failed after ...` | **동일** |

**`ReconciliationBatch` 60초·target 60 수리는 실측으로 효과가 확인됐다.**
recovery 조건 `checkout_5xx_rate_baseline < 0.05`를 이제 여유 있게 만족한다.

**그런데 레지스트리 `_note`에 내가 남긴 가설 — "평시 재고가 0에 닿지 않으면 이 dirty도 함께 풀릴 것"— 은 거짓이다.**
전제(재고 0 해소)는 참으로 확인됐지만 결론이 틀렸다. **dirty의 원인이 409가 아니라 companion 종료 검증이었다.**
`_note`의 recovery 조건 셋은 마지막 틱에서 **전부 만족**한다 — 실패한 것은 컨트롤러 recovery가 아니라 **실행기 recovery**다.
→ **`_note` 갱신 대상.** 조건 자체는 그대로 두는 게 맞고(전역 계약 판단은 유효), **인과 서술만 정정**한다.
※ entry 409가 31% 남은 원인은 미조사.

### 실측 — 왜 간헐적인가가 완전히 설명된다 (확정)

`load.north_south` **전환** cleanup 소요(`intent_at` → `complete_at`), 전 런 전수:

| n | min | p50 | **p90** | **max** |
|---|---|---|---|---|
| **88** | 1.44s | 1.66s | **4.42s** | **7.57s** |

**max 7.57초가 바로 F25-H의 그 건이다**(20:45:44.695 → 20:45:52.262).
**F25-H는 88건 중 역대 최장이었고 그 끝에서 검증이 실패했다.** p50이 1.66초라 나머지 87건은 지나갔다.

### 소요가 아니라 **검증 방식**의 문제다 (확정)

다른 프로파일의 전환 cleanup 소요:

| 프로파일 | n | p50 | max | recovery 검증 |
|---|---|---|---|---|
| `k8s.env` | 18 | 68.19s | 82.84s | `rollout` + `healthy 1s` — **기다린다** |
| `k8s.patch` | 10 | 61.26s | 66.35s | 〃 |
| `k8s.resource` | 11 | 42.19s | 68.21s | 〃 |
| `cache.control` | 7 | 1.96s | 2.28s | `rollout status --timeout=1s` — **기다린다** |
| **`load.north_south`** | 88 | 1.66s | 7.57s | `[[ -z "$(tagged_pids)" ]]` — **즉시 단정** ⚠ |
| **`host.stress`** | 25 | 2.30s | 3.59s | `! alive` — **즉시 단정** ⚠ |

**k8s 계열은 60~80초가 걸려도 실패하지 않는다. north_south는 1.66초짜리인데 실패한다.**
k8s는 "상태를 묻고 기다리고", 프로세스 계열은 "지금 없어야 한다"고 단정한다. **그런데 프로세스 종료는 비동기다.**

### 영향 범위 — **19종** (사다리 2단+ × north_south 사용, 확정)

live 16 + parked 3. 음성 대조(사다리인데 north_south 미사용)는 `F15-T1` 하나뿐.
```
F02-H F03-H F03-P F05-P F05-R F07-H* F09-H F09-P F09-R F10-H
F10-P F11-R* F12-H F15-P F15-R F20-R* F21-P F25-H          (* = north_south가 사다리 소유)
```
**단, 실제로 승격이 일어날 때만 밟는다.** 오늘 승격을 겪은 north_south 런들(F12-H 2회·F09-R 2회·F05-P 1회)은
전부 p50 구간이라 무사했다.

`host.stress`도 같은 형태이고 **사다리 × host.stress = F02-H·F05-P·F09-R·F10-H·F10-P·F15-P 6종**이 잠재 노출이다
(미발현 — 실측 max 3.59s로 짧다). **두 실행기를 함께 고치면 사다리 20종 중 19종이 덮인다.**

### 수리
1. **R1 (이번 주기 가능)** — `load.north_south` recovery의 `tagged_pids` 검사를 **즉시 판정 → 폴링·타임아웃**으로.
   `host_stress_executor.py`의 `! alive`도 **함께** 고친다 — 미발현이지만 형태가 같고 비용이 같다.
   지금 안 고치면 fio 종료가 느린 날 똑같이 겪는다.

   > **타임아웃 = 30초. 폴링 간격 0.5~1초.**
   > 에이전트는 15초(max 7.57s의 2배)를 권하며 30초도 합리적이라 했다. **30초를 택한다.**
   > 비용이 비대칭이기 때문이다 — 타임아웃이 길어서 손해 보는 것은 **드문 꼬리 사례에서 몇 초**뿐이고
   > (k8s 전환이 이미 60~80초라 전환 프로파일에 영향 없음), 짧아서 손해 보는 것은
   > **dirty 런 하나 + 시터 액션 1회 + 약 10분**이다. 게다가 표본 88건의 max가 7.57초라 **더 긴 꼬리를 배제할 수 없다.**
   > 반증법: 수리 후 폴링 실제 소요를 로그로 남겨 재분포.

   **dirty는 시터 액션을 소비한다** — 오늘 2회 소비됐다. R1이 그 소모를 막는다.
   ※ `db.lock`/`db.workload`의 `no_clients`도 대기가 없다. kubectl 파드 삭제가 동기라 위험이 낮아 보이나 **미확인.**

---

## C-2. 레지스트리 불일치 — `injected_fault` vs `profiles.json` (확정, 신규)

`load.north_south`를 실제로 쓰는 시나리오를 세는 데 두 소스가 갈린다:

- `scenario-metadata.json`의 `injected_fault.companions`/`profile` 기준 → **41종**
- `profiles.json`의 `load.north_south.scenario_parameters` 기준 → **50종**

**9종이 metadata에서 누락돼 있다**: `F01-G F02-P F03-G F05-G F07-H F11-G F11-R F20-R F24-Q`
- `F20-R`·`F07-H`는 `companions: []`인데 **north_south가 그 사다리를 소유한다**
- `F11-R`은 `companions: ['cache.control']`만 있고 north_south가 빠져 있다

**집계를 metadata로 하면 틀린다.** 양성 대조군(F20-R)을 넣어 검증하다가 잡혔다 — 대조군 규칙이 또 한 번 값을 했다.

§C(F18-P의 `infra_anchor` vs `injection_summary` 모순)와 **같은 계열**이다. 4면 동기화 대상에 함께 넣는다.
※ 어느 쪽이 정본인지는 미판단.

> **앞선 §B 보고의 "north_south 41종"은 과소집계다(실제 50종).**
> **다만 B 수리의 결론은 영향받지 않는다** — 그건 `script_path` 기준이었고 k6 스크립트 9종을 직접 읽어 판정했다.
> 수리 대상은 여전히 banking 3파일(F10-P·F15-P·F18-P·F21-P)이다. 숫자만 정정한다.
2. **R2 (실측 선행)** — 사다리 첫 단 재설계. 192Mi로 안 죽으니 실제로 죽는 값을 찾아야 한다.
   ⚠ **F05-P의 교훈이 적용된다**: 너무 낮추면 기동 중 OOM으로 트래픽을 한 건도 못 받고 crashloop한다
   (F05-R success `_note`가 같은 함정을 기록). **"부하 중 OOM은 나되 기동은 되는" 구간을 실측으로 찾을 것.**
   **R1 → R2 순서** — R1을 안 고치면 사다리를 올릴 때마다 또 dirty가 난다.
3. **별도 항목**: 전환을 원자적으로 만든다(새 레벨 먼저 적용 후 이전 정리, 또는 전환 구간을 판정에서 제외).
   러너 판정 로직 변경이라 무겁다.

---

## O. F03-H — F19형 재발. **단 "강도 의존적"이다.**

`F03-H-run-d7efaa9d` (2026-08-07 21:33:09Z~22:00:16Z UTC, calibration), `aborted / must_rule_out_detected`, 레벨 2.

발화 조건 (`must_rule_out.any`, `consecutive_ticks: 2`):
```json
{"id": "baseline-arm-also-failing", "observation": "checkout_5xx_rate_baseline", "op": "gte", "value": 0.02}
```

| elapsed | entry | order_p95 | checkout_5xx | **baseline** | ready |
|---|---|---|---|---|---|
| +366s | 502 | 18,635 ms | 0.807 | **0.250** | True |
| +381s | 502 | 32,720 ms | 0.961 | **1.000** | False |

`min_hold` 6m 만료 **직후 첫 두 틱**이다(F18-P와 같은 패턴).
다른 조건은 미발화(`entry_status==429` 실제 502, `achieved_rps<15` 실제 30.8/38.3).
**`pod_ready`는 must_rule_out에도 abort에도 없다.**

### 주입 정체 — 사다리 소유는 `load.east_west`다
```
load.east_west    levels=3   ← 사다리 소유. 클러스터 내부 k6 job이 order 스레드풀을 포화시킨다
load.north_south  levels=0   ← companion (고정)
```
레벨명의 30/45/60rps는 **east_west** 부하량이다.
베이스라인이 오염되는 이유는 north_south 포화가 아니라 **east_west가 order를 죽여서**
baseline 로드젠의 체크아웃도 그 order를 지나기 때문이다. → **F19형(대조군이 주입 구성요소를 공유) 확정.**

### 강도 의존적이라는 것이 핵심이다

| 레벨 | order_p95 | checkout_5xx | **baseline** |
|---|---|---|---|
| L0 `below-30rps` (36틱) | 15~904ms (대부분 40~60) | **0.000 전부** | **0.000 전부** |
| L1 `at-45rps` (36틱) | 16~375ms | **0.000 전부** | **0.000 전부** |
| **L2 `past-60rps` (23틱)** | **10,082~32,720ms** | 0.218~1.000 | **0.250~1.000** |

**주입이 작동하는 그 순간 대조 팔도 함께 무너진다.**

이력을 보면 `_note`의 관측("baseline은 228 표본 전체에서 0.000")은 **틀리지 않았다 — 그 강도에서는 참이었다**:

| run | 결과 | 도달 레벨 | max baseline | **max order_p95** |
|---|---|---|---|---|
| 2b2bd916 (08-05) | aborted `maximum_injection_duration` | past-60rps | **0.0** | 6,877 |
| a4875aa9 (08-07 06:22) | 〃 | past-60rps | **0.0** | 2,904 |
| **d7efaa9d (오늘 21:33)** | **`must_rule_out`** | past-60rps | **1.0** | **32,720** |

**과거 두 런도 L2까지 갔다.** 차이는 강도다 — order_p95가 **4.7~11배** 뛰었고 그때 처음 대조 팔이 무너졌다.

> **§F05-P 게이지 교훈과 같은 형태다: 대조 팔을 약한 주입에서 검증하는 것은 검증이 아니다.**
> 대조 팔이 필요한 순간은 주입이 실제로 작동할 때이고, 오염은 바로 그때만 드러난다.

### 강도가 11배 뛴 원인 — **우리가 올렸다** (확정)

**`32c4308` (2026-08-07T07:52:46Z): "F03-H (run a4875aa9): 최상위 단의 target_url days 120→240"**
커밋 시각이 a4875aa9(06:22) **이후**, d7efaa9d(21:33) **이전**이다.

세 런의 `load.east_west` 레벨 파라미터 전문 대조 — **딱 한 곳이 다르다:**

| run | L0 | L1 | **L2** |
|---|---|---|---|
| 2b2bd916 (08-05) | `days=120` | `days=120` | **`days=120`** |
| a4875aa9 (08-07 06:22) | `days=120` | `days=120` | **`days=120`** |
| **d7efaa9d (오늘 21:33)** | `days=120` | `days=120` | **`days=240`** |

나머지는 전부 동일(`target_rps` 30/45/60, `max_vus` 360, `duration_seconds` 480, `node_name` tb-w1).
**L0·L1이 세 런 모두 무효였는데 L2만 벌어진 것도 이것으로 설명된다 — 바뀐 것이 L2뿐이다.**

**왜 2배가 11배가 되는가** (`OrderReportRenderer.java:35,53,68-74`):
```java
public synchronized String render(int days) {   // 전체 synchronized — 동시 요청 직렬화
    int points = days * BUCKETS_PER_DAY;        // n = days × 상수
    for (int i = 0; i < n; i++) for (int j = 0; j <= i; j++)   // O(n²)
```
`days` 2배 → n 2배 → 렌더 비용 **4배**(O(n²)) → `synchronized` 모니터 서비스율 **1/4** →
도착률은 60rps 고정이라 **큐가 폭발**한다. 4배 비용이 11배 지연이 된 것은 큐잉 비선형성이다(가설, 강함).

### 후보 셋은 전부 반증 (확정)

| 후보 | 판정 | 근거 |
|---|---|---|
| 앱 변경(`ReconciliationBatch`) | **반증** | 렌더러 교체는 `10fe748`(**07-28**)로 세 런 모두 이전. 게다가 렌더러의 `series`는 **합성 데이터**라 DB를 안 탄다 |
| 테이블 성장(orders 1,185,713행) | **반증** | 같은 이유 — 렌더러가 DB를 안 탄다. n은 `days`에서만 나온다 |
| **동시 실행 오염** | **반증** | **배치 32런 중 실제 `t1~t2` 겹친 쌍 0개** |

> **`overlapping_run_ids`를 오독했다.** 그건 "동시 실행 중"이 아니라 **"clean-window 안에 *끝난* 런"** 목록이다.
> F03-H의 baseline clean_window는 `21:03:08~21:33:08`(t1 소급 30분)이고,
> F15-R은 21:28:00, F23-R은 21:04:27에 **종료**했다(t1 21:33:09 이전).
> `clean-window: false`는 그 창이 완전히 조용하지 않았다는 뜻이지 병렬 실행이 아니다.
> **→ 배치 무결성 문제 없음. §G 통과 감사 재검토 불필요.**

### 재해석 — `32c4308`은 실패가 아니라 **성공**이다

1. 근접 실패(2,904ms)를 보고 L2 강도를 4배로 올렸다 → **의도대로 작동했다**
2. 주입이 처음으로 제대로 걸렸다(order_p95 32,720ms, checkout_5xx 1.0)
3. **그 강도에서 처음으로 대조 팔이 오염됐다**(baseline 0.0 → 1.0)
4. `baseline-arm-also-failing`이 발화해 중단

**강도 수리가 가려져 있던 F19형 결함을 노출시킨 것이다.**
`_note`의 "228 표본에서 baseline 0.000"도 거짓이 아니라 **그 강도에서 참이었던 관측**이다.

※ 같은 커밋의 F25-H 수리(첫 단 256Mi 제거)는 **불충분했다** — 192Mi에서도 restart 16틱 내내 0이었다(§N).

### 처방 — **§F19 수리에 F03-H를 함께 넣는다** (백로그 #11)
같은 계약(`7c90e08`), 같은 기전. 대안은 전부 기각:
임계 상향(L2 baseline이 0.25~1.0이라 0.5로 올려도 발화) / success 성립 시 면제(감별자 의미 소멸 + 평가 순서 의존) /
대조 팔을 주입 경로 밖으로(**불가능 — checkout이 곧 order다**).

※ F19와 다른 점: **L0/L1에서는 대조 팔이 깨끗하다.** 그 관측 자체는
"주입이 약할 때는 order가 안 죽는다"는 정보이므로 **success의 강도 판정 쪽에서 살릴 수 있다.**

### 부수 확정 셋

1. **전환 비원자성 서명은 특이적이다.** 내가 `level_changes: 0`이라 잘못 읽어 넘겼는데 **실제로는 3개**이고
   승격 2회가 정상 기록됐다. 그 서명(`level_index` 증가 + `level_changes` **미기록**)은
   F25-H·F20-R·F03-P에서만 나타난다. **§N 계열 정의는 영향받지 않고 오히려 특이성이 확인됐다.**

2. **`order_p95` 32,720ms는 실재하고 §M-2와 무관하다.** 트레이스 L2: n=514, p50 6,219ms, **p95 26,372ms**,
   분당 p95 최댓값 **33,195ms** — 게이지 32,720과 거의 일치. 표본도 분당 54~129로 충분.
   → **§M-2에 내가 적어둔 반증법("표본이 충분한 서비스에서 수렴하는지")의 실증이다. 표본 부족 가설이 강화된다.**
   (F09-R은 분당 4~42 표본에서 2~3배 어긋났다.)
   ※ L2에서 order의 **ERROR 스팬은 0**이다 — order는 지연만 있고 5xx는 게이트웨이가 타임아웃으로 만든다.

3. **L0·L1이 무효인데 27분 중 20분을 먹는다.** `order_p95` 40~60ms는 성공 임계 2,000의 **1/40**이다.
   승격은 escalate가 아니라 **레벨 timeout(10m) 만료**로 일어났다(21:33:09→21:43:17, 21:43:18→21:53:25).
   **F25-H(192Mi)·F20-R(rps 1)과 같은 "사다리 첫 단 무효" 패턴 — 세 번째다.**
   사다리 설계 관행 자체를 볼 가치가 있다(미조사).

---

## P. F10-H — **여덟째 계열: 베이스라인 환경 결함형.** 사례가 이미 셋이다.

`F10-H-run-54b9fd46` (2026-08-07 22:19:24Z~22:24:39Z UTC, calibration), `aborted / must_rule_out_detected`, 레벨 0.

```json
{"id": "baseline-arm-also-failing", "observation": "order_create_5xx_rate_baseline", "op": "gte", "value": 0.2}
```
`consecutive_ticks: 3` — 발화 세 틱 전부 **baseline 0.500**.

**나머지 세 감별자는 발화 불가였다**(전 17틱): `disk_io_util < 30`(최소 27이 1틱뿐, 나머지 34~65) /
`node_cpu_util >= 85`(최대 77.1) / `achieved_rps > 60`(최대 31.1). **유일한 발화 조건이다.**

### F19형이 **아니다** — 오염 원인이 주입과 무관하다

조건의 `_note`가 오염 원인을 직접 밝힌다:
> *같은 food 도메인의 대조 팔이 지금 0.93~1.00이다 — **courier 풀 포화가 주입과 무관하게** 주문 생성을 503으로 만든다.*

**주입은 디스크 IO이고 오염 원인은 courier 풀이다. 둘은 무관하다.** 실측이 뒷받침한다 —
`disk_io_util`이 레벨 0 내내 34~45(success 임계 60 미달)인데 baseline은 el=163부터 이미 0.2~0.667로 오염돼 있다.
**주입이 약한 구간에서도 대조 팔이 더럽다. F03-H와 정반대다**(F03-H는 주입이 세질 때만 오염됐다).

### 계열 정의

> **베이스라인 환경 결함형**: 주입과 무관한 이유로 베이스라인 환경이 **상시** 고장나 있어 대조 팔이 항상 오염된다.
> **감별자가 정직하게 작동할수록 시나리오는 통과 불가가 된다.**

| | §F19형 | **§P 베이스라인 환경 결함형** |
|---|---|---|
| 오염 원인 | **주입**(구성요소 공유) | **주입과 무관한 환경 결함** |
| 발현 | 주입이 성공할 때 | **항상** |
| 강도 의존 | 예(F03-H 실증) | 아니오 |
| **처방** | **감별자 제거** | **환경을 고친다 — 감별자는 죄가 없다** |

### 사례가 이미 셋 — 세 `_note`가 같은 말을 한다

| 시나리오 | 환경 결함 | `_note` |
|---|---|---|
| **F10-H** | courier 풀 포화 → food 주문 503 | "주입과 무관하게" |
| **F25-H** | commerce 입구 baseline 409 | "그쪽에서 고쳐야 한다" |
| **F05-R** | commerce 재고 고갈 → checkout 409 | "이 시나리오 밖에서 고쳐야 한다" |

**"이건 이 시나리오의 문제가 아니다"** — 셋 다 같은 구조다.
F05-R 재고 고갈은 `52f23b6`로 수리됐고 F25-H 409도 86%→31%로 개선됐다(§N).
**courier 풀 포화만 남아 있다.**

### 감별자가 승격을 가로챘다 (추가)
`streaks: {escalate: 5, must_rule_out: 3, success: 0}`, `level_changes: 1`(레벨 0에서 종료).
`escalate`(`disk_io_util < 60`) 스트릭이 5까지 찼는데 승격 전에 감별자가 3연속으로 먼저 중단시켰다.

**레벨 0의 `disk_io_util`은 34~45로 success 임계 60에 못 미치고, `_note`는 이전 런에서
최상위(io-20000)에서야 62~78이었다고 기록한다.** → **승격했으면 success가 섰을 가능성이 있다**(가설).
감별자가 그 기회를 먼저 잘랐다.

### 이력 — 4런 0승. 다만 **위장이 아니라 원인이 실제로 두 번 바뀌었다**
```
bc1bf9ba(08-03) / ffb01cd5(08-05) / 748dd9c8(08-07 07:11)  → calibration_levels_exhausted, io-20000 도달
54b9fd46(오늘 22:19)                                        → must_rule_out_detected, 레벨 0에서 종료
```
F20-R·F09-R은 **하나의 만족 불가능한 조건**이 사인을 바꿔가며 나타난 것이지만, F10-H는 원인이 진짜로 바뀌었다:
1. 이전 3런: `order_p95`의 `update_interval 60s`가 게이트 전체를 60초 페이스에 묶어 3연속이 안 섰다 — **판정 페이스 문제**
2. `f60c903`이 성공 신호를 `order_create_5xx_rate`(30초 케이던스)로 교체해 **고쳤다**
3. 그 수리가 대조 팔 없는 절대 임계라는 **새 위험**을 들였다
4. 그것을 막으려 `baseline-arm-also-failing`을 신설했다 — 그런데 **대조 팔이 이미 더러웠다**

**수리가 새 문제를 만든 연쇄이고, 각 단계는 옳았다. 마지막에서 환경 결함에 부딪혔다.**
그리고 `_note`는 이 결과를 **예견하면서 fail-closed를 택했다 — 의도대로 작동한 것이다.**

### 수리
- **R1(근본): courier 풀 포화를 고친다.** F05-R 재고 고갈·F25-H 409와 같은 **베이스라인 자립성** 층이다.
  **원인·규모 미조사 → 이번 주기 가능 여부 판단 불가.** 조사 선행.
- **R2: 그때까지 감별자는 유지한다.** 대조 팔이 더러우면 귀속 불가가 맞다.
  **통과 불가는 환경이 고장났다는 정직한 신호이지 시나리오의 결함이 아니다.**
  → **R1 전까지 F10-H는 parked가 맞다.** 돌려봐야 같은 자리에서 죽는다.
- **R3(별건): success의 `disk_io_util >= 60`이 레벨 0에서 도달 불가**(34~45, 최상위에서야 62~78).
  사다리를 끝까지 올려야만 성립하는데 감별자가 레벨 0에서 자르면 영원히 못 간다.
  **success 임계를 레벨별로 다르게 둘 수 있는지**가 설계 질문으로 남는다(러너 문법 지원 여부 미확인).

---

## Q. F09-H — **아홉째 계열: 예산 소진형.** 수리는 한 줄이고 선례가 있다.

`F09-H-run-9d46c50f` (2026-08-07 23:04:01Z~23:34:40Z UTC, calibration),
`aborted / maximum_injection_duration`, 레벨 4(heap-96). **`streaks` 통째로 비어 있다 — 아무 조건도 발화하지 않았다.**

### 전환 서명 **없음** — 5단을 전부 밟았는데도
```
도달 레벨 5 (전 단), level_changes 5개 전부 정상 기록
heap-208 453s / heap-160 454s / heap-128 453s / heap-112 454s / heap-96  21s ← 예산 만료
```
**가장 깊은 사다리인데 승격 4회가 무사했다.** §N 전환 비원자성 서명의 특이성이 다시 확인된다.

### 원인 — 예산 산술이 안 맞는다
```
max_injection_duration = 30m = 1800초
실제 총 소요           = 1839초       ← 초과
레벨당 실소요 453초 vs 예산 나눗셈 1800/5 = 360초/레벨  → 93초씩 초과
```

### 오늘 아침 성공 런과의 대조가 결정적이다 (같은 시나리오·같은 사다리·같은 날)

| | **밤 `9d46c50f`** | **아침 `93ec85bc`** |
|---|---|---|
| 결과 | **aborted**(예산) | **succeeded** |
| 총 소요 | **1,839초** | **1,661초** |
| 레벨당 | 453 / 454 / 453 / 454 / **21** | 336 / 331 / 329 / 329 / **332** |
| 레벨당 틱 | 22 | 14 |

**레벨당 122초씩 더 걸렸고 5단에서 누적돼 예산을 넘겼다. 여유는 139초뿐이었다.**

**판정 지연이 아니다** — 두 런 모두 하위 4단에서 `escalate`(`ratio < 0.7`)가 전 틱 성립했고 unusable 0, 진동 없음.
신호값도 거의 동일하다(0.480→0.392→0.509→0.572→0.674).
시간을 먹은 것은 **`k8s.env` 롤아웃**으로 보인다(전 런 전수에서 transition이 61~83초로 최장, 5단이면 4배로 받는다). **가설.**

### 성공은 최상위 단에서만 성립한다
아침 런의 success는 **heap-96에서만** 섰다(0.807). 하위 4단은 전부 `< 0.7`이었다.
**오늘 밤은 그 단에 21초만 남아 evaluating 틱이 0개였다.**
→ **예산이 유일한 성공 기회를 잘랐다.** §P·§F15-P의 "감별자가 승격을 가로챈다"의 사촌이되 **가로챈 것은 예산이다.**

### 계열 — §J(부하 수명 소진)과 구분된다

| | **§J 부하 수명 소진** | **§Q 예산 소진** |
|---|---|---|
| 무엇이 끝나나 | **k6 수명** | **러너의 주입 예산** |
| 관측 | **사라진다**(프로브 파일 소실) | **멀쩡하다** |
| 사인 | `*_observation_unavailable` | `maximum_injection_duration` |
| 수리 지점 | k6 `hold` / 러너 판정 로직 | `max_injection_duration` / 사다리 길이 |

### 구조적 실패가 아니다 — 이력 4런 **1승** 3패
```
4793c9f4(08-03) aborted must_rule_out, heap-208   4cc52147(08-06) aborted must_rule_out, heap-160
93ec85bc(08-07 07:57) succeeded, heap-96          9d46c50f(오늘 밤) aborted 예산, heap-96
```
**오늘 아침 같은 사다리로 성공했다.** 롤아웃이 조금만 느려지면 넘어가는 형태다.
※ 앞선 두 중단은 사인이 달라 **원인도 다를 가능성** — 미조사.

### 수리 — R1은 이번 주기에 넣을 만하다
**`max_injection_duration` 30m → 40m 이상.** 최악 산술은 5단 × timeout 8m = **40분**이고
현재 예산은 최악의 **75%**밖에 못 덮는다.

> **선례가 있다.** `32c4308`이 F05-P를 4단으로 늘리며 예산을 30m→32m로 올렸고 커밋에 이렇게 적었다:
> *"단이 4개가 되어 최악 32m이므로 max_injection_duration도 30m→32m —
> 예산이 사다리를 못 덮으면 무릎을 찾기도 전에 런이 끝난다."*
> **그 판단이 F09-H(5단, 예산 30m 그대로)에는 적용되지 않았다.**

부작용: 런이 길어져 배치 시간이 는다. 다만 **최악을 안 덮으면 같은 일이 반복된다.**

**R2(대안, 뒤로)**: 사다리 단축. 하위 4단이 전부 `< 0.7`로 무효였다(두 런 모두) —
heap-208·160은 힙이 넉넉해 아무 일도 안 일어난다. `32c4308`이 F25-H에서 무효 첫 단을 뺀 것과 같은 처방.
**단, 단을 빼면 "무릎"을 놓칠 위험이 있다** — calibration의 목적이 그 지점을 찾는 것이다.
**R1을 먼저 하고 실측이 쌓인 뒤 R2를 판단할 것.**

### ★ 전수 점검 결과 — **여유율 100%는 안전하지 않다** (확정)

사다리 20종 산술(대조군 F09-H 포함 검증):

| 시나리오 | 단 | 예산 | 최악(단×timeout) | **여유율** | min_hold+settle 합 |
|---|---|---|---|---|---|
| **F15-R** | 2 | 1800 | 2880 | **62%** | **2220** ⚠ |
| **F09-H** | 5 | 1800 | 2400 | **75%** | 1350 |
| **F05-P** | 4 | 1920 | 1920 | **100%** | 1080 |
| **F03-H** | 3 | 1800 | 1800 | **100%** | 1170 |
| F21-P(parked) | 3 | 1800 | 1620 | 111% | 1080 |
| F07-H / F11-R | 3 | 2400 | 1980 | 121% | 1170 / 450 |
| 나머지 12종 | 3 | 1800 | 1440 | 125% | 810~990 |
| F05-R | 2 | 1800 | 960 | 188% | 300 |

#### `maximum_injection_duration` 이력 — **3건, 전부 최상위 단에서**
```
F03-H-run-2b2bd916  08-05 03:39  1817초  past-saturation-60rps (idx 2)
F03-H-run-a4875aa9  08-07 06:22  1811초  past-saturation-60rps (idx 2)
F09-H-run-9d46c50f  08-07 23:04  1839초  heap-96 (idx 4)
```
**셋 다 사다리를 끝까지 올라가 놓고 그 단에서 예산이 만료됐다.**

#### 판정 기준을 고쳐야 한다
**F03-H는 여유율 정확히 100%인데 실측이 1811·1817초로 최악 산술(1800)을 넘었다.**
**최악 산술이 전환 시간을 포함하지 않기 때문이다:**
```
실제 = Σ(레벨 소요) + Σ(전환 소요)
전환 실측: k8s.env 61~83초 / k8s.patch 41~66초 / host.stress 1~4초 (3단이면 2회, 5단이면 4회)
```
→ **여유율은 `예산 / (최악 + 전환 총합)`으로 계산하고 최소 110~120%는 되어야 한다.**
이 기준이면 F21-P(111%)도 전환 2회를 더해 105%로 경계에 든다. 125% 그룹은 전환을 더해도 12% 이상 남는다.

#### 산술 위험 대 실측 위험 — 구분해야 우선순위가 선다

| | 여유율 | 실측 | 실제 발생 |
|---|---|---|---|
| **F03-H** | 100% | 1627 / **1811** / **1817** | **2건** |
| **F09-H** | 75% | 323 / 656 / **1661(성공)** / **1839** | **1건** |
| F15-R | 62% | 1099·1113(성공) / 1403·1409·1410 — **전 런 `changes=1`** | **없음** |
| F05-P | 100% | 365 / 550 (둘 다 일찍 abort) | 없음 |

> ### ★ F15-R 위험도 정정 (2026-08-07 23:50) — **1위가 아니다. 조건부 2차 위험이다.**
>
> | 근거 | |
> |---|---|
> | 5런 전부 `level_index=0`·`changes=1` | **승격이 한 번도 일어나지 않았다** |
> | `64390fc`(08-06) 수리 후 **2런 연속 성공**(1099·1113초) | 현재 건강하다 |
> | 성공 소요 1099·1113 대 예산 1800 | **여유 63%** |
>
> **승격 조건이 `checkout_5xx_rate < 0.02` 4틱 연속이다 — 주입이 잘 작동하면 5xx가 올라가 승격하지 않는다.**
> **즉 예산 위험은 "주입이 실패했을 때만 발현"하고, 그때는 어차피 다른 문제가 먼저다.**
>
> **`min_hold` 18분은 의도된 설계이고 줄일 수 없다** (`64390fc` 명시):
> 에피소드 2의 fault 창(`fault 240 + gap 720 + fault 240` → 960~1200s) **안쪽**이어야 하고 1080초가 그 중앙이다.
> success가 `mock_flap_episode >= 2` AND `checkout_5xx_rate >= 0.05`이므로 **2번째 fault가 진행 중일 때 판정해야 한다.**
> 직전 값 21분(1260초)은 창 밖이라 *"성공 증거가 있는데 선언 자체가 불가능"*했다.
> **축소 가능폭은 최대 120초 — 예산 부족분(420초)을 못 메운다.**
>
> **`safety_observation_unavailable` 3건은 이미 수리됐다.** 전부 08-05이고 `64390fc`가 08-06에 고쳤다.
> 원인은 **§J형이 맞다** — 다만 부하가 아니라 **flap 주입 워커**가 마지막 에피소드 restore 직후 종료해
> `mock_flap_*`이 1233초부터 stale이 됐다(evaluating 틱 실측: 그 둘만 8~9/76~77 unusable, 나머지 신호는 전부 usable).
> → **§J의 정의가 "부하"가 아니라 "주입 산출물"로 넓어져야 한다.** 수리 방향도 §12와 같다
> (`64390fc`: *"워커가 마지막 에피소드도 recovery_gap을 hold해 관측면이 판정 창보다 오래 산다"*).
> **일반 계약("주입 산출물의 수명 ≥ 판정 창")이 있었으면 F20-R도 함께 막혔을 것이다.**
>
> **수리 판단(내 결정)**: **지금 사다리를 줄이지 않는다.**
> 에이전트가 `episode-fault-300`이 5런 내내 미사용이므로 1단 축소를 제안했고 예산 문제가 사라지는 건 맞지만,
> **그 단이 존재하는 이유가 미조사**이고 **calibration의 목적이 사다리로 무릎을 찾는 것**이다.
> 조건부 2차 위험을 피하려고 calibration의 본질을 버리는 건 교환이 맞지 않는다.
> **승격이 실제로 일어나면 그때 예산을 올린다**(최악 48분이라 50분 필요 — 배치 부담이 크므로 발현 후 판단).
>
> ~~**⚠ F15-R이 잠재 위험 1위다.**~~ (아래는 정정 전 기록) 5런 전부 **승격을 한 번도 안 해서** 62%인데도 안 걸렸다.
> **`min_hold + settle` 합만 2220초로 이미 예산 1800초를 넘는다** — **승격하는 순간 구조적으로 완주 불가**다.
> min_hold 18분 × 2단은 이례적이다(다른 시나리오는 4~6분). **예산 상향만으로 되는지 재검토 필요 — 설계 의도 미조사.**
> ※ F15-R의 `safety_observation_unavailable` 3건(1403~1410초)은 레벨 timeout 1380초 **직후**라 **§J형 후보**로 보인다. 미조사.

### ★ 분류 정정 — **F03-H는 두 계열에 걸쳐 있다**

§O에서 F03-H를 **F19형(대조 팔 오염)**으로만 분류했다. **과거 두 런은 그게 아니다.**
당시 `_note`를 인용해 "`order_p95`가 60초 페이스에 묶여 3연속이 안 섰다 — 판정 페이스 문제"라고 썼는데,
**그 설명은 왜 success가 안 섰는지는 맞지만 왜 런이 끝났는지는 설명하지 않는다.**
두 런 모두 1811·1817초로 **예산이 만료돼 끝났다.**

| 런 | 계열 |
|---|---|
| `d7efaa9d` (오늘) | **F19형** — 대조 팔 오염 (§O, 유효) |
| `2b2bd916`·`a4875aa9` (과거 2) | **예산 소진형** (§Q) |

**→ F03-H는 §O와 §Q 양쪽에 등재한다.**

### 수리 우선순위 (전수 결과 반영)

| # | 대상 | 현재 | 권고 | 근거 |
|---|---|---|---|---|
| **1** | **F03-H** | 30m / 3단×10m | **35m 이상** | **2회 발생.** 실측 1817 + 전환 여유 |
| **2** | **F09-H** | 30m / 5단×8m | **40m 이상** | 1회 발생. 최악 2400 + 전환 4회 |
| ~~3~~ | ~~F15-R~~ | 30m / 2단×23m | **보류 — 승격 시 판단** | 정정: 조건부 2차 위험. min_hold는 설계상 축소 불가, 현재 2런 연속 성공 |
| 4 | F05-P | 32m / 4단×8m | 관찰 | 여유 0이나 실측이 짧다 |

**1~2는 레지스트리 한 줄씩. 3은 설계 확인 선행.**

#### 부수 제안 — 가드
사다리를 바꿀 때 예산을 함께 검사하면 `32c4308`이 F05-P에만 적용하고 F09-H를 놓친 일이 반복되지 않는다:
```
assert max_injection_duration >= Σ(level timeout) + Σ(예상 전환 시간) × 여유계수
```

⚠ **다른 사다리 시나리오의 예산도 같은 산술로 점검할 것**(단 수 × 레벨 timeout 대 `max_injection_duration`).

---

## 별도 항목 (이번 주기에 넣지 않음)

1. **공유 아웃박스 테이블에 대한 무스코프 `findPending`** — 항목 제목을 "Oracle 스키마 분리"로 잡지 말 것.
   해법 후보 넷: 서비스별 스키마(commerce 방식) / 서비스별 테이블(food 방식) /
   `aggregate_type` 스코프(공유 테이블 유지, DDL·이관 없음) / 릴레이 통합.
   **`aggregate_type` 스코프가 가장 쌀 수 있다 — 미검증 추측이므로 실측 후 판단.**
2. **보존 정책** — `banking.transfers` 980,498행 / `food.dispatches` 1,369,619행.
   오늘 **테이블 성장이 시나리오 판정을 뒤집은 사례가 둘**이다. 선택 항목이 아니다.
   F20 계열 재보정(2.32× 드리프트)도 여기에 묶인다.
3. **k6 스크립트가 다이제스트 보호 밖에 있다** — 실행기 `.py`는 `executor_sha256`으로 고정되는데
   실제 부하를 결정하는 `.js`는 안 된다. **보호 경계가 실제 인과 경계와 어긋나 있다.**
   부하 정의가 조용히 바뀌어도 런 산출물로는 감지할 수 없다 — 오늘 겪은 것과 같은 계열(주입은 걸렸는데 관측이 못 봄).
   안: `allowed_script_paths`에 경로만이 아니라 기대 sha256을 함께 싣고 실행기가 대조.
   ※ 이번 수리가 바로 이 공백 덕에 싸게 끝난다는 점은 아이러니다. 수리 후에 닫을 것.

4. **F21-P 제어 표면 비대칭** — `response_delay_control`도 `service_id` 키 단일 행이고 `transfer`만 값이 있다.
   지연은 필터 기반이라 아웃박스 공유와 무관하지만 같은 비대칭 위에 서 있다.
   F21-P 승격 전 이 표면이 대상 서비스에서만 유효한지 확인 필요. (미조사)

---

## 예측 — **정정됨**

### 틀린 예측 (기록으로 남긴다)
> "F10-P는 주입과 무관하게 `transfer_2xx_rate < 0.95`로 must_rule_out 중단된다."

**틀렸다. F10-P에는 `sync-path-also-broken` must_rule_out이 없다.** `controllers.json` 실측:

```
F10-P  success        [{"id":"transfer-2xx-collapse", "observation":"transfer_2xx_rate", "op":"lt", "value":0.5}]
       must_rule_out  [{"id":"user-load-overshot",    "observation":"achieved_rps",      "op":"gt", "value":60}]
```

`transfer_2xx_rate < 0.5`는 F10-P의 **성공 조건**이다. F18-P에서 부하만으로 실측된 2xx는 **0.30~0.49** — 그 임계 아래다.

### 정정된 예측
**F10-P는 중단되지 않고 `succeeded`로 판정될 가능성이 높다. 주입(fio)이 걸렸든 안 걸렸든.**

> ### ★ 결과 (2026-08-07 22:41) — **전반부 적중, 후반부는 지지받지 못했다**
>
> `F10-P-run-188c3300` (t1 22:29:49Z, t2 22:40:57Z) → **`succeeded`**. 중단되지 않았다.
> **전반부(중단이 아니라 성공)는 맞았다.**
>
> **후반부(그 성공이 거짓 양성)는 틀린 쪽으로 기울었다.** 사전 등록(`prereg-f10p-f15p-0807.md`) A절 기계적 적용 결과:
>
> | 잣대 | 결과 |
> |---|---|
> | 레벨 단조성 | M0=50 → M1=60.5, 차이 **10.5** → 확정(≥15)·없음(<5) 사이 = **모호** |
> | 절대 포화(≥85 연속 2틱) | **거짓**(85+ 단 1틱) |
> | 선후 | `T_disk` 정의 불가 → **판정 불가** |
> | **account 대조** | **9.4 → 1,357.7 → 10.9 ms (144×)** — **주입 원인을 강하게 지지** |
>
> → **등록 규칙상 「판정 유보」(규칙 4).** 다만 거짓 양성 확정 규칙은 둘 다 "급등 없음"을 요구하는데
> 급등은 "없음"이 아니었고, **account 144배는 부하로 설명되지 않는다**
> (F18-P 실측: 무제한 조회는 transfer만 때리고 `/api/accounts*`는 거의 전량 200).
> **Oracle 인스턴스 자체가 느려졌다는 뜻이고 그것은 디스크 주입의 직접 결과다.**
>
> **실질적 귀결: §B(무제한 조회 수리)의 시급성 근거로 F10-P를 쓸 수 없다. F18-P·F15-P 쪽으로만 남는다.**
>
> ※ 등록 절차의 설계 결함도 함께 기록했다 — **15pp 기준이 3레벨 스팬 전제인데 이 런은 2레벨만 밟았다.**
> 레벨당 10.5pp씩 오르던 중이라 io-20000까지 갔으면 "확정"이 나왔을 수 있다.
> **결과를 보고 기준을 바꾸지 않는다.** 별도 개정 대상으로 남긴다.

**거짓 중단보다 나쁘다.** 중단은 눈에 보이지만 거짓 성공은 유효 표본으로 조용히 데이터셋에 들어간다.

### 자체 감별자가 못 막는 이유 — 일반화할 만한 맹점
F10-P의 must_rule_out은 `achieved_rps > 60`이고 target_rps는 20이다. 감별자는 통과한다.

**감별자가 부하의 *양*을 보고 *건당 비용*을 안 보기 때문이다.**
무제한 조회는 rps를 그대로 둔 채 요청 하나의 원가만 **2,572배**로 올린다(10.0 MiB vs 4,089 B 실측).
`achieved_rps` 계열 감별자 전부가 이 형태에 눈이 멀어 있다. — 오늘 확인된 40개 감별자 조건 재검토 시 이 각도를 넣을 것.

### 그래서 볼 것 — "중단되는가"가 아니라 "성공이 주입 때문인가"

F10-P는 판별에 필요한 신호를 이미 관측한다. **`disk_io_util`과 `transfer_hikari_pending`의 선후:**

| 원인 | `disk_io_util` | `hikari_pending` | 순서 |
|---|---|---|---|
| 주입(fio)이 진짜 원인 | **급등** | 상승 | disk가 **먼저**, hikari가 따라온다 |
| 부하(무제한 조회)가 원인 | **평온** | 상승 | disk는 안 움직이고 hikari만 오른다 |

`disk_io_util` 평온 + 2xx가 0.5 아래 → **거짓 양성 확정.**

**F10-P 자신의 must_support 1번("PVC 백킹 디바이스 busy 비율 급등")이 곧 판별식인데 success 조건에 안 들어 있다.**
이게 이 시나리오의 별도 결함이다. → 수리 대상.

**중요**: F10-P가 안 멈춘다고 부하 가설이 반증되는 게 아니다. `disk_io_util` 평온 + 2xx 붕괴면 **확증**이다.
