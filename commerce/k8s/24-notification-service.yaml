apiVersion: apps/v1
kind: Deployment
metadata:
  name: testbed-notification
  namespace: rca-testbed-commerce
  labels:
    app: testbed-notification
spec:
  replicas: 1
  selector:
    matchLabels:
      app: testbed-notification
  template:
    metadata:
      labels:
        app: testbed-notification
    spec:
      containers:
        - name: notification-service
          image: commerce-notification:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 8084
          envFrom:
            - configMapRef:
                name: service-config
            - secretRef:
                name: postgres-secret
          env:
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: postgres-secret
                  key: POSTGRES_USER
            - name: DB_PASS
              valueFrom:
                secretKeyRef:
                  name: postgres-secret
                  key: POSTGRES_PASSWORD
          startupProbe:
            httpGet:
              path: /actuator/health
              port: 8084
            failureThreshold: 30
            periodSeconds: 5
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8084
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8084
            periodSeconds: 15
            timeoutSeconds: 3
            failureThreshold: 5
          resources:
            requests:
              cpu: 200m
              memory: 512Mi
            limits:
              cpu: 500m
              memory: 1Gi
---
apiVersion: v1
kind: Service
metadata:
  name: testbed-notification
  namespace: rca-testbed-commerce
spec:
  selector:
    app: testbed-notification
  ports:
    - port: 8084
      targetPort: 8084
