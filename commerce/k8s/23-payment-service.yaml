apiVersion: apps/v1
kind: Deployment
metadata:
  name: testbed-payment
  namespace: rca-testbed-commerce
  labels:
    app: testbed-payment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: testbed-payment
  template:
    metadata:
      labels:
        app: testbed-payment
    spec:
      containers:
        - name: payment-service
          image: commerce-payment:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 8083
          envFrom:
            - configMapRef:
                name: service-config
            - secretRef:
                name: postgres-secret
          env:
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: postgres-secret
                  key: POSTGRES_USER
            - name: DB_PASS
              valueFrom:
                secretKeyRef:
                  name: postgres-secret
                  key: POSTGRES_PASSWORD
          startupProbe:
            httpGet:
              path: /actuator/health
              port: 8083
            failureThreshold: 30
            periodSeconds: 5
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8083
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8083
            periodSeconds: 15
            timeoutSeconds: 3
            failureThreshold: 5
          resources:
            requests:
              cpu: 200m
              memory: 512Mi
            limits:
              cpu: 500m
              memory: 1Gi
---
apiVersion: v1
kind: Service
metadata:
  name: testbed-payment
  namespace: rca-testbed-commerce
spec:
  selector:
    app: testbed-payment
  ports:
    - port: 8083
      targetPort: 8083
