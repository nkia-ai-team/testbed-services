apiVersion: apps/v1
kind: Deployment
metadata:
  name: testbed-shipping
  namespace: rca-testbed-commerce
  labels:
    app: testbed-shipping
spec:
  replicas: 1
  selector:
    matchLabels:
      app: testbed-shipping
  template:
    metadata:
      labels:
        app: testbed-shipping
    spec:
      containers:
        - name: shipping-service
          image: commerce-shipping:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 8088
          envFrom:
            - configMapRef:
                name: service-config
            - secretRef:
                name: postgres-secret
          env:
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: postgres-secret
                  key: POSTGRES_USER
            - name: DB_PASS
              valueFrom:
                secretKeyRef:
                  name: postgres-secret
                  key: POSTGRES_PASSWORD
          startupProbe:
            httpGet:
              path: /actuator/health
              port: 8088
            failureThreshold: 30
            periodSeconds: 5
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8088
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8088
            periodSeconds: 15
            timeoutSeconds: 3
            failureThreshold: 5
          resources:
            requests:
              cpu: 200m
              memory: 512Mi
            limits:
              cpu: 500m
              memory: 1Gi
---
apiVersion: v1
kind: Service
metadata:
  name: testbed-shipping
  namespace: rca-testbed-commerce
spec:
  selector:
    app: testbed-shipping
  ports:
    - port: 8088
      targetPort: 8088
